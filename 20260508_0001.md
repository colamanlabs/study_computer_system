
#### [71/134] Flash Memory 2
```aiignore
1. Flash Memory 관리기법

1) 수명연장 기법 Wear Leveling
- wear 는 마모라는 의미, leveling 은 평준화 의미  
- Dynamic Wear Leveling : 특정영역(USB)
    - write 할 때만 웨어라벨링 
    - 일단 write 된 데이터는 그대로 두므로, 리텐션 이슈로 장기보관시 데이터 오류 가능성 존재 -> 단기적재용 
- Static Wear Leveling : 전체영역(SSD)
    - 전체 영역 웨어라벨링
    - 리텐션 이슈가 dynamic 보다 낮음

- 플래시 메모리의 수명제한을 극복하기 위해 블록당 write 횟수를 모니터하여, 한 블록에만 치우치지 않도록 균등하게 분배하는 기능
- 마모 평준화를 하지 않았을 경우, 사용한 곳만 계속 사용하는 경우가 발생하여 블록에 더이상 기록을 할 수 없는 상태에 도달함 

2. 삭제 효율을 위한 가비지 컬렉션
- 디스크 작업에 따라 Garbage page(invalid 로 표기된 페이지)의 증가가 발생하여, 적절한 시점에 Garbage Page 를 한번에 처리하는 기술

3. 웨어 라벨링과 가비지컬렉션을 위한 공간. 오버 프로비저닝 공간
- 비어있는 여유공간이 부족할 시 웨어 라벨링과 가비지 콜렉션 제대로 수행 불가. 이를 극복하기 위한 여유공간 

4) OOB(Out of Band)
- SSD/NAND Flash 에서 OOB(Out Of Band)는 데이터 영역 바깥에 따로 존재하는 “부가 정보 저장 영역”
ECC(Error Correcting Code)
Bad Block Marker
LBA 매핑 정보
Page 상태 정보
Wear Leveling 관련 정보
GC 관련 정보

cf) LBA (Logical Block Addressing) - 블록 디바이스 논리주소 
- 블록 주소 방식
- 현재는 48bit LBA 이상 
- 클러스터는 파일시스템 에서 사용하는 크기(4K 이면 512 블록 8개)
- LBA 는 디스크 전체 주소(파티션 기준이 아님)

Application
→ FileSystem
→ LBA
→ Device Firmware(FTL)
→ Physical Media 
```



#### [72/134] Flash Memory 3(파일시스템)
```aiignore

```


#### [73/134] Flash Memory 4(Flash Translation Layer)
```aiignore
- 플래시 메모리와 파일시스템 사이에 위치, 논리적 파일 시스템과 물리적 플래시 매모리를 매핑시켜 DISK 처럼 사용하게 하는 계층 
- 블록/페이지 기반의 SSD 를 섹터 기반의 HDD 처럼 인식시키기 위한 SSD 의 핵심기술
```


#### [74/134] SSD
```aiignore
- NAND 플래시 또는 DRAM 등 초고속 반도체 메모리와 컨트롤러의 결합으로 구성된 대용량 저장장치
- IO 성능 증대, 고용량 저장장치, 빠른 속도, 저전력, 표준 인터페이스(ATA, IDE, SCSI 등) 사용

- SSD 를 아주 넓게 정의하면 "회전 플래터 없는 반도체 저장장치"
    - 기계식 아님, 반도체 사용, block device 로 동작
    
- SSD 제작유형
1) 플래시 기반 SSD
2) DRAM 기반 SSD 

- SSD 구성요서
1) Host Interface
2) Process : FTL 수행
3) SRAM
4) DRAM
5) Flash Controller
6) Memory Block
```


#### [75/134] FeRAM / STT-MRAM / PRAM
```aiignore
1. 강유전체(Ferroelectric RAM) 기반 FeRAM/STT-MRAM/PRAM
- 강유전체를 이용한 비휘발성 메모리로 낮은 전력소모, 빠른 속도, 쓰기/지우기 속도 향상(1000배)을 도모한 차세대 메모리
-  강유전체
    - 자연상태에서 전기편극을 가지고 있는 물질
    - 자발분극을 나타내는 이 물질들이 전기장 속에서 분극의 방향이 바뀌지 않는 것은 제외된다. 
    - 이렇게 분극이 변하는 것에 의해 압전성과 초전성을 띈다.

2. FeRAM/STT-MRAM/PRAM 의 구성요소
```


#### [76/134] CAM(Content-Addressable Memory)
```aiignore
CAM(Content-Addressable Memory) 은 “주소로 찾는 메모리”가 아니라 내용으로 찾는 메모리입니다.

일반 메모리 RAM은 보통 이렇게 동작합니다.
주소 입력 → 해당 주소의 데이터 출력

CAM은 반대로 동작합니다.
찾고 싶은 데이터 입력 → 그 데이터가 들어 있는 위치 출력

예를 들어 CAM 안에 다음 값들이 있다고 하면:
0번: 1010
1번: 1100
2번: 0111
3번: 1001

입력값으로 1100을 넣으면 CAM은:
1번 위치에 있음

이라고 알려줍니다.

대표적인 사용처는 TLB, CPU 캐시 태그 비교, 네트워크 라우터의 주소 검색입니다. 
- 특히 TLB에서는 “가상 페이지 번호가 이미 등록되어 있는가?”를 빠르게 찾기 위해 CAM 구조를 사용합니다.

정리하면:
RAM  = 주소로 데이터 검색
CAM  = 데이터 내용으로 주소/위치 검색

CAM은 검색이 매우 빠르지만, 회로가 복잡하고 전력 소모가 커서 일반 대용량 메모리로 쓰기보다는 고속 검색용 소규모 하드웨어에 주로 사용됩니다.
```



#### [77/134] Storage(DAS/NAS/SAN)
```aiignore
1. Storage
- 대용량의 데이터를 저장, 활용하기 위해서 구성된 일종의 외장형 대용량 데이터 저장소

2. 유형
1) DAS
2) NAS 
3) SAN : 서버가 Fiber Channel SAN switch 를 통해 storage 를 연결하는 기법(storage 전용 네트워크를 구성)
4) IP-SAN : IP 프로토콜 기반의 네트워크로 SAN 스토리지에 연결하여 데이터를 접근하는 기술(외부접근, Infiniband) 
```



#### [78/134] IP-SAN
```aiignore
1. IP-SAN
SAN 에 IP 기술을 접목하여 기존 SAN 의 거리제약 문제를 극복한 원거리 data 전송/저장 기술

2. IP-SAN 종류
1) FCIP(Fiber Channel Over IP) : FC SAN 구축을 위한 TCP/IP 내의 FC 프레임 캡슐화
2) iFCP(internet Fiber Channel Protocol) : FC 스토리지 장비를 직접 IP 네트워크 확장 가능
3) iSCSI(Internet SCSI)
- TCP/IP 와 SCSI 프로토콜을 결합해 SCSI 명령을 TCP/IP 패킷으로 캡슐화 하고, IO 블록 데이터가 IP 네트워크를 통해 전달되는 표준기술
4) FCoE(Fiber Channel over Ethernet)
- 점보 이더넷(10G) 프레임 내에 파이버 채널 프레임을 캡슐화 하여, 이더넷을 통해 파이버채널 프레임을 전송하는 기술 
```



#### [79/134] RAID(Redundant Array of Independent Disk)1
```aiignore
1. RAID
- 여러개의 하드디스크를 묶어 스트라이핑과 redundancy 를 적용하여 하나의 저장장치처럼 사용하는 기법 

2. 구현원리
1) Data Striping : 데이터를 여러 조각으로 나누어서 여러 디스크에 저장
2) Redundancy : 일부 데이터 손실시 복구 가능토록 데이터 중복 저장 

3. RAID 종류와 기술특징
- 0, 1, 2, 3, 4, 5, 6, 01, 10, 51

4. 하이브리드 RAID
- 01 ,10, 51

5. RAID 구성방법
1) software 기반 : OS 가 제공기능 이용구성, 논리적/물리적 구성가능 / 비용 저렴 / CPU 성능저하
2) hardware 기반 : bios 기능 이용구성 / sw 기반 구성에 비해 성능우수 / 부하가 적음 / 모든 RAID 완벽하게 구현하지 못함. BIOS 에 종속
3) Firmware/driver 기반 : RAID 컨트롤러가 존재하는 외장장치 기반으로 구성 / 성능 : 가장 우수, 모든 RAID 완벽지원 / 최대 비용소요
```

| 구분 | 특징                        | 최소수량 | 공간효율  | 읽기    | 쓰기       | 고장허용     |
|----|---------------------------|------|-------|-------|----------|----------|
| 0  | 스트라이핑                     | 2    | 1n    | n배 상향 | n배 상향    | zero     |
| 1  | 미러링                       | 2    | 1/n   | n배 상향 | 1배 상향    | n-1      |
| 2  | bit 레벨 스트라이핑<br/>해킹코드 패리티 | 3    | -     | 해당없음  | 해당없음     | 1 disk   |
| 3  | byte 레벨 스트라이핑<br/>패리티     | 3    | 1-1/n | 해당없음  | 해당없음     | 1 disk   |
| 4  | block 레벨 스트라이핑<br/>패리티    | 3    | 1-1/n | 해당없음  | 해당없음     | 1 disk   |
| 5  | block 레벨 스트라이핑<br/>분산 패리티 | 3    | 1-1/n | n-1배  | variable | 1 disk   |
| 6  | block 레벨 스트라이핑<br/>더블 패리티 | 4    | 1-2/n | 해당없음  | 해당없음     | 2 disk   |
| 01 | 스트라이핑 -> 미러링              | 4배수  | n*2/d | -     | -        | 0쌍 아닌 2개 |
| 10 | 미러링 -> 미러링                | 4배수  | n*2/d | -     | -        | 1내에 하나   |
| 51 | 1은 spare                  | 6    | -     | -     | -        | 2        |

#### [80/134] RAID(Redundant Array of Independent Disk)2
```aiignore

```

#### [81/134] SATA(Serial Advanced Technology Attachment)
```aiignore
1. 직렬 전송 방식의 버스 인터페이스, SATA
- PATA(Parallel) 신호 간섭, 노이즈, 신호의 도착시간 불일치, 높은 전압등의 한계를 극복하기 위해 개발된 직렬 전송방식, 버스 인터페이스

2. 특징
1) 직렬전송 : 신호의 도착시간 일치 및 회로 간소화
2) P2P 연결 : 케이블당 하나의 장치로 PATA 의 master/slave 개념 불필요
3) 저전력 : PATA 에 비해 1/10 수준의 낮은 전압 사용, 신호 간섭과 노이즈 발생 제거
4) NCQ(Native Command Queuing) : HDD 에 들어오는 명령어에 우선순위를 배당해 재배열
5) HOT-SWAP : 전원이 들어온 상태에서도 USB 처럼 탈착 가능

3. 개념 및 구성요소(Serial ATA 는 각 디바이스를 P2P 방식으로 각자의 케이블을 이용해 연결)
1) SATA 어댑터 : 메인보드에 장착된 SATA 컨트롤러
2) 장치 드라이버 : SATA 어댑터를 제어하기 위한 장치 드라이버
3) SATA Connector : 디바이스 종류(2.5인치/3.5인치 HDD)에 상관없이 동일한 커넥터 사용
4) 전원 Connector : 웨이퍼 기반의 15핀 
```



#### [82/134] Disk Scheduling 
```aiignore
1. 디스크 스케쥴링
- 사용할 데이터가 디스크상의 여러곳에 저장되어 있을 경우 데이터를 액세스 하기 위해 디스크 헤드가 움직이는 최적의 경로를 경정하는 기법

2. Seek time
= 탐색시간(헤드가 지정된 트렉에 도착하는 시간) + 회전지연시간(원하는 섹터까지 이동시간) + 전송시간(Data 전송 소요 시간)

3. 디스크 스케쥴링 유형
1) Seek Time 최적화 : 디스크 IO 를 위한 실린더, 트랟접근시간, 접근경로 최적화 / 이동식 디스크 / SSTF, SCAN
2) latency 최적화 : 섹터까지의 이동시간, 이동경로 최적화 / 고정식 디스크 / SLTF, Sector Queuing

4. 디스크 스케쥴링 유형에 따른 알고리즘
1) FCFS (First Come First Served)
- 요청큐에 들어온 순서대로 처리 / 알고리즘 단순, 구현용이, 공정한 스케줄링 기법 / 비효율적

2) SSTF (Shortest Seek Time First)
- 현재 헤드위치에서 가장 가까운 트랙의 요청처리 / 쓰루풋 극대화, 시크타임 최소화 / 안쪽 바깥쪽 트랙 starvation

3) SCAN (엘리베이터 알고리즘)
- 진행방향의 가장 가까운 트랙의 요청을 먼저 서비스 / SSTF의 기아해결, SSTF 의 응답시간 편차 해결 / 끝 트랙은 대기시간 길어짐

4) C-SCAN(Circular SCAN) : 항상 바깥쪽에서 안쪽 SCAN / 응답시간의 편차가 적음 / 안쪽이나 바깥쪽으로 처리요청이 없어도 끝까지 감

5) Look  
- SCAN 과 같이 처리하되, 처리할 블록이 없으면 끝까지 가지 않고 돌아옴 / 불필요 헤드 이동시간 제거 / 진행여부 결정위한 오버헤드

6) C-Look 
- C-SCAN 과 같이 처리, 처리할 블록이 없으면 끝까지 가지않고 돌아옴 / 불필요 헤드 이동시간 제거 / 진행여부 결정 오버해드

7) SLTF (Shortest Latency Time First ) 
- 고정식 / 회전시간 최적화 기법 / 주로 드럼과 같은 고정 헤드 장치를 스케쥴링 할 때

8) Sector Queuing 
- 고정식 / 고정헤드 디스크 알고리즘 / Sector 번호에 의한 회선시간만 고려


6. 디스크 스케쥴링 효용성
1) 평균 Seek Time 효율성 : SSTF > SCAN > C-SCAN > FCFS
2) Fairness 효율성 : FCFS > C-SCAN > SCAN > SSTF
3) Disk heavy load 인 경우 : C-SCAN > SCAN > SSTF 

 
```
