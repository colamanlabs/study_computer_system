
#### 2. 컴퓨터 구조 및 적용기술
##### 2.3 메인메모리

```aiignore
키워드
# 메모리 인터리빙
# 메모리 듀얼채널 
# SCM(Storage Class Memory) -> 옵테인 메모리
# DMA
# Program driven IO -> PIO 모드 
# Interrupt driven IO
# DMA

 
# 페이지 교체 알고리즘
# FIFO (First-In First-Out)
# LRU (Least Recently Used) -> locality 잘 반영
# LFU (Least Frequently Used)
# MFU (Most Frequently Used)
# Second Chance (Clock) -> 참조 비트(Reference Bit) 를 두고 최근 사용이력 있으면 1 로 하고, 다시 기회를 준다. FIFO 개선.
# Clock (원형 큐) -> Second Change 를 실제 구현한 형태 
```

#### 3. 폰노이만 아키텍처/하바드 아키텍처
```aiignore
키워드
# 폰노이만 아키텍처 
# 명령과, 데이터를 한 메모리에 저장 

# 하바드 아키텍처
# 폰노이만의 메모리 접근 병목을 개선, 명령어와 데이터터 메모리가 분리되어 병렬적으로 작업 처리
```


#### 4. 컴퓨터 구성요소
```aiignore


```

#### 6. CPU 성능지표
```aiignore
키워드
# 응답시간 : 초 
# 시간당 처리량 : TPS, OPS  
# 자원사용량 : %
# 효율성 : tpmC (transactions per minute C) "C" 는 TPC-C 워크로드(실제 업무 시나리오 기반 DB 성능 벤치마크)
# 응답시간(Response Time) = “사용자가 처음 반응을 느끼기까지 시간”
# 반환시간(Turnaround Time) = “작업이 완전히 끝날 때까지 전체 시간”
# 가용도 : 주어진 자원중 사용자가 그 자원을 사용할 수 있는 확률  
# 신뢰도 : 장애가 발생하지 않을 확률 
```

#### 7. 멀티코어/매니코어
```aiignore

```s

#### 8. CISC/RISC
````aiignore

````

#### 9. RISC-V 
```aiignore
RISC 기반 개방형 ISA 
- 개방형 표준 : BSD 라이센스 (누구나 수정가능)
- 모듈성 : 기본 ISA + 표준확장 조합 
- 확장성 : 사용자 정의(커스텀) 명령어 추가 허용
- RISC 원칙 : 단순 고정길이 명령어

- 상용화 현황 : 저전력, 컨트롤러, AI 가속기등 확대
- 기술적 제약(생태계 성숙도) : ARM 대비 검증 SW, 최적화 컴파일러등 미성숙
- 기술적 제약(고성능, 보안등) : 고성능 영역 최적화, 사용등급 보안 기술검증 진행중   
```

#### 10. ARM(Advanced RISC Machine)
```aiignore
CISC 대비 명령어수를 획기적으로 줄인 임베디드, 모바일 기기에서 사용되는 저전력 RISC 프로세서    
```


#### 11. EISC 
```aiignore
    
```


#### 12. EPIC
```aiignore
    
```

#### 13. 명령어 사이클 
```aiignore
    
```


#### 14. 명령어 주소지정 방식(Addressing Mode)
```aiignore
    
```


#### 15. 병렬컴퓨터1
```aiignore

    
```
