
#### 15. 병렬컴퓨터1
```aiignore
1. 플린분류
- 스탠퍼드 전기공학과 교수, 마이클 J 플린, 1966 에 제안
- instruction stream 과 data stream 의 개수에 따라 구분
- SISD (single instruction stream,single data stream)
- SIMD (single instruction stream,multiple data stream) : MMX, SSE, AVX, 하나의 제어장치와 여러개의 ALU 로 구성 
- MISD (multiple instruction stream,single data stream) : 한번데 데이터 1개를 여러 명령어로 처리, CPU 파이프라인 기법  
- MIMD (multiple instruction stream,multiple data stream) : 여러 데이터를 다른 instruction 으로 한꺼번에 처리, 현대의 동시적 멀티스레드는 모두 MIMD 다.


2. 공유메모리 공유(강결합)
- SMP(Symmetric Multiprocessing)
    - 장점 : 부하분산
    - 단점 : 메모리병목, 캐시일관성 
    - 현대의 멀티코어 CPU 는 SMP 구조다. (8 코어 CPU -> 8개의 SMP 프로세서), OS 가 스케줄링
- UMA 
- NUMA


3. 병렬 처리 구성시 문제점, 해결방안
- 분할문제 : Task 의 분할단위를 어떻게 할 것인가 -> 병렬성 탐지, 묶음
- 스케쥴링 문제 : 실행할 프로세서의 배정 -> 정적(컴파일시 컴파일러가 배정), 동적 스케쥴링
- 동기화 문제 : 공유데이터 변경되지 못하도록 하는 작업 -> 버스잠금, 플레그, 세마포어 기법
- 캐시메모리 문제 : 적절한 캐시 사용을 통한 확장  
```



#### 16. 병렬컴퓨터2
```aiignore
1. 공유 메모리 시스템 형태의 병렬 컴퓨팅(강결합구조)
- SMP : 단일 처리기 시스템에서 2개 이상의 프로세서를 공유버스로 연결, 하나의 메모리 망에 연결
- UMA : 모든 프로세서들이 상호 연결망에 의해 접속된 주기억장치 공유
- NUMA : 프로세서별 공유메모리, 시스템 버스 사용없이 데이터 access, 고유기억장치 
 
2. 분산 메모리 시스템 형태의 병렬컴퓨팅(약결합구조)
- MPP(Massively Parallel Processors) : 각 노드별 각각의 CPU 와 전용메모리로 구성되며, 메모리 공유 안함
- LCMP : SMP 를 클러스터 노드로 상호 연결해 놓은 구조
- CC-NUMA(Cache-Coherent NUMA) : 모든 노드들의 캐시 및 주기억장치들 사이에 데이터 일관성 유지
- COMA (Cache-Only Memory Architecture) : 각 프로세스가 가지고 있는 기억장치들이 모두 캐시로서 동작하며, 하나의 공통주소공간을 가짐 
```

#### 17. 병행성과 병렬성
```aiignore
1. 병행성(Concurrency)
- 하나의 프로세서를 이용해 두개 이상의 프로그램이 동시에 실행되는 성질. 한 순간에는 한개의 프로그램만 처리됨
- 성질 : 프로그램의 성질
- 동시수행대상 : 프로세스
- 구현사례 : 멀티 프로그래밍, 멀티 프로세싱, CPU 스케줄링, 컨텍스트 스위칭 
- 문제점 : 프로세스 동기화
- 해결방안 : 세마포어, 모니터

2. 병렬성(Parallelism)
- 복수 프로세스가 여러개 또는 한 프로그램의 분할된 부분을 동시에 처리하는 성질. 한순간에 두개 이상의 프로그램이 처리
- 성질 : 기계의 성질 
- 동시수행대상 : 연산 
- 구현사례 : 멀티 프로세서, 멀티 스레딩, 파이프라인 컴퓨터, 어레이 컴퓨터
- 문제점 : 프로세서 동기화
- 해결방안 : 버스락, 스핀락, 베리어
```


#### 18. PCB(Process Control Block)
```aiignore
PCB : 프로세스를 관리하기 위해 유지되는 데이터 블록 또는 레코드 데이터 구조
TCB : Thread 실행시간동안 상태정보 유지하기 위해 관리되는 데이터 구조
```

| -        | PCB                                                              | TCB                                                       |
|----------|------------------------------------------------------------------|-----------------------------------------------------------|
| 역할       | 프로세스 정보저장<br/>모든 Thread 에 공유되는 정보                                | 쓰레드 정보저장<br/>쓰레드 내에서 사용되는 정보                              |
| 주요구성요소   | Owner, PID, Proccess Stack Pointer<br/>, Priority, Active Thread | Thread Stack Pointer, Thread PC, Thread Register          |
| 상호연계정보   | 하나이상의 TCB 정보                                                     | 쓰레드가 속한 PCB, 쓰레드간 링크정보                                    |
| 컨텍스트 관점  | 전체 쓰레드의 실행환경 정보                                                  | 쓰레드별 실행 관련 정보                                             |
| 관리 데이터 량 | 관리 데이터 많음<br/>TCB 에서 공유정보 포함(메모리 관리정보, 오픈파일등)<br/>리눅스기준 106개 필드  | 상대적으로 적은 데이터 관리(TCB 는 PCB 에 연결 포인터가 있음)<br/>리눅스 기준 24개 필드<br/> |


#### 19. 명령어 기반의 성능향상(파이프라이닝, 스칼라, VLIW)
```aiignore
FI(Fetch Instruction) -> DI(Decode Instruction) -> EI(Execute Instruction) -> WB(Write Back)

수퍼 파이프라인, 수퍼 수칼라, VLIW 공통적으로 "명령어 병렬성에 따라 성능이 좌우된다."
```

| -        | Super Pipeline                                                  | Super Scalar                                                   | VLIW            |
|----------|-----------------------------------------------------------------|----------------------------------------------------------------|-----------------|
| 명령어 해독   | 별개해독<br/>동시인출                                                   | 별개 해독<br/>동시인출                                                 | 하나의 회로에서 인출, 해독 |
| 명령어 밀도   | 클럭 주파수에 좌우                                                      | ALU 수에 좌우 | 병렬성에 따라 좌우      |
| 병렬성 검출   | 실행시 | 실행시 | 컴파일시 | 


#### 19. PCB(Process Control Block)
```aiignore
PCB : 프로세스를 관리하기 위해 유지되는 데이터 블록 또는 레코드 데이터 구조
TCB : Thread 실행시간동안 상태정보 유지하기 위해 관리되는 데이터 구조
```

#### 20. 파이프라인 해저드(Pipeline Hazard)
```aiignore
파이프라인 기법의 성능을 저해하는 요인
1. 구조적 해저드
- 2개이상의 명령어가 동일한 하드웨어 자원을 동시요구(자원충돌)
- 대책 :
    - 하바드 아키텍처 : 명령어와 데이터 분리, 충분한 자원확보, 해당 기능 사용시까지 지연 

2. 데이터 해저드
- 선행 명령어가 사용하는 데이터와 후행 명령어가 사용하는 데이터의 종속관계, 파이프라인을 멈춰야 하는 상황(데이터 의존)
- RAW(Read After Write) : 선행 명령어가 후행 명령어의 데이터를 읽기 전에 쓰는 경우
- WAR(Write After Read) : 선행 명령어가 후행 명령어의 데이터를 쓰기 전에 읽는 경우
- WAW(Write After Write) : 선행 명령어가 후행 명령어의 데이터를 쓰기 전에 또다시 쓰는 경우
- 대책 :
    - 의존성 문제 해결시 까지 다음 명령어 실행 지연 
    - Forwarding 기법 : 계산결과 바로전달
    - Restrict software : 컴파일러에서 nop 명령 삽입 or 영향없는 범위내 순서변경
    - Change Clock Cycle : 레지스터에 앞쪽 1/2 클럭에 쓰고, 뒤쪽 2/2 클럭에서 읽음
 
3. 제어 해저드
- 분기문등으로 실행할 명령어가 겨정되지 않아 파이프라인을 멈춰야 하는 상황
- 대책 :
    - 분기판정을 조기에 수행
    - 지연분기 : 분기 손실되는 클럭동안 다른 명령 수행
    - 예측분기 
```

#### 21. 워치독 타이머
```aiignore
- 타임체크를 통한 시스템 리셋, 워치독 타이머
- 외부잡음이나 비정상적인 루프나 무한루프등, 사용자에 의해 더이상 통제가 불가능한 상황시 시스템을 리셋하는 명령어  
```


#### 22. JTAG(Joint Test Action Group)
```aiignore

```


#### 23. Context Switching(문맥교환)
```aiignore
- 멀티 프로세스 환경에서 실행중인 프로세스 상태를 보관하고, 새로운 프로세스의 상태를 CPU에 적재하는 과정
- 관련용어
1) Dispatching : 준비 상태의 프로세스들 중에서 우선순위가 가장 높은 프로세스에게 CPU 를 할당하여 CPU 로 진입
2) Time Quantum(Slice) : 특정 프로세스에게 할당되는 시간의 단위, 독점방지
3) Preemption(프리엠프션, 선점) : Time Quantum 을 초과하면 인터럽트를 통해 CPU 사용권을 빼앗는 스케쥴링 기법

- 문맥교환상황
1) 자발적 문맥교환 : sleep, exit등  
2) 비자발적 문맥교환 : time slice 소진시, 인터럽트 발생시

- 컨텍스트 스위칭 오버헤드 대응방안
1) 다중 프로그램의 정도를 낮춤
2) 스택 중심 시스템에서는 스택 포인터 변경을 통한 프로세스간 문맥교환 수행 
3) 스레드를 이용한 컨텍스트 스위칭 부하 최소화
```

#### 24. Process
```aiignore
프로세스 상호작용
[경쟁]
1) 임계영역 : 공유자원을 접근하는 병행작업이 불가능한 코드 세그먼트 구간
2) 상호배제 : 어느지점에도 한 프로세스만 임계영역이 존재
3) 상호 block : 기아상태(임계영역 독점), 교착상태(무한대기), live lock(양보, 무한연기)

[협동]
1) 동기화 : 프로세스 우선 순위 부여
2) 공유메모리 : 공유메모리를 통한 정보공유
3) 메세지 전송 : 메세지 송수신을 통한 임계영역 공유
4) 세마포어 : P, V, 초기화 연산을 통한 동기화 문제 해결

프로세스 상태전이
running stage
ready state
block state
```


#### 25. Process 동기화
```aiignore
프로세스가 자원을 동시에 접속할 때 발생하는 문제를 해결하기 위해 프로세스간 실행순서와 규칙을 정의하고, 프로세스 상태의 일관성을 보장하는 기법

프로세스 동기화 기법

[Locking 매커니즘]
1) Bus Locking : 프로세서가 버스에 접속된 공유자원을 엑세스 하는 동안에는 버스를 독점함으로서, 다른 프로세서들이 버스를 사용하지 못하게 하는 방식
- Bus Request -> 버스 사용허가 획득 -> Bus 잠금 -> 임계영역에서 실행 -> 버스 잠금 해제
2) Bus Lock : Lock 변수 상태를 지속적으로 검사하여, 허가된 경우에만 임계영역을 실행하도록 하는 방식(Busy Waiting)
3) 커널모드 동기화, 세마포어
4) 모니터

[H/W 기법]
1) 인터럽트 금지 : 공유변수가 변경되는 동안 인터럽트 발생을 허용하지 않음
2) Test & Set : H/W 에서 한 word 의 내용을 검사하고 변경하는 명령어의 제공
3) SWAP : H/W 에서 두 워드간의 내용을 원자적으로 교환할 수 있는 명려엉 제공

동기화 구현의 Trade off
- 프로세스간 실행순서 보장, 데이터 일관성 vs dead lock, starvation, 우선순위 역전   
```


#### 26. IPC
```aiignore
- IPC : 프로세스간 통신
- IPC 필요성
1) 정보공유
2) 계산가속화
3) 모듈성
4) 편의성 

- 프로세스간 통신방법
1) pipe 
2) shared memory 
3) message queue(OS MQ)
```

#### 27. 원격 프로시저 호출(Remote Procedure Call) 
```aiignore
- 별도의 원격제어를 위한 코딩 없이 다른 주소 공간에서 함수나 프로시저를 실행할 수 있게하는 프로세스간 통신기술
- IDL : “서로 다른 시스템(언어/OS) 간에 통신하기 위해 함수의 ‘약속(인터페이스)’만 기술하는 언어”, 중립적인 ‘함수 명세서’
- IDL 컴파일러가 코드를 자동 생성
- Java RMI는 전통적인 의미의 IDL을 사용하지 않습니다. 대신 Java 인터페이스 자체가 IDL 역할을 합니다.
```

| 용어       | 원래 의미      | RPC에서의 역할     | 느낌    |
| -------- | ---------- | ------------- | ----- |
| stub     | 잘린 조각, 대리물 | 클라이언트 쪽 호출 대행 | 얇은 앞단 |
| skeleton | 뼈대, 골격     | 서버 쪽 호출 처리 구조 | 내부 구조 |



#### 28. CPU Scheduling
```aiignore
CPU 스케쥴링 운영기준
1) Throughput
2) Turnaround time
3) waiting time
4) response time
5) CPU utilization(CPU 사용율)
6) Fairness : 프로세스별 CPU 자원할당의 공정성
7) Deadline : 실시간 환경등에서의 즉시처리 한계시간
```



#### 29. Scheduling 기법 및 알고리즘 
```aiignore
1. 선점 스케줄링 
- 우선순위가 높은 프로세스가 현재 프로세스를 중단하고 CPU 차지 가능
1) RR
2) SRT(Short Remaining TIme) : 가장 짧은 시간 소요의 프로세스 먼저 선점
3) 다단계 큐(Multilevel Queue) : 
- 작업들을 여러종류의 그룹으로 분할, 상위단계 선점, 각 큐마다 스케쥴링 방식이 다를 수 있다. 낮은 우선순위 큐의 starvation 가능성 존재함
- 큐이동 거의 없음 
- 프로세스를 종류별로 고정분류하는 방식
4) 다단계 피드백 큐(Multilevel Feedback Queue, MLFQ) : 
- 우선순위별 여러 큐를 둔다. 
- 프로세스가 큐를 이동한다. 
- 짧게 CPU 를 쓰고 자주 대기하는 대화형 작업은 높은 우선순위를 유지, CPU 를 오래 점유하는 계산작업은 낮은 우선순위로 이동   
- 프로세스의 실제 CPU 사용 패턴을 보면서, 동적으로 우선순위를 조정하는 방식 
- 대화형 시스템에서 특히 유리

2. 비선점 스케줄링
- 하나의 프로세스가 CPU 할당 받으면 완료시 까지 다른 프로세스 할당 불가
1) 우선순위 스케쥴링 : 우선순위에 따라 CPU 할당
2) 기한부 스케줄링 : 명시된 시간이나 기한내 완료
3) FCFS(First Come First Service) : FIFO 알고리즘
4) SJF(Shortest Job First) : 수행시간 짧은 것 먼저 수행
5) HRN(Highest Response Ratio Next) : 대기중인 프로세스중 Response Ratio 가 가장 높은 것  
```


#### 30. Real time Scheduling 
```aiignore
1. 실시간 스케쥴잉 알고리즘 특징
1) 선점형 : 높은 우선순위를 갖는 task 에 CPU 양보
2) 우선순위 : task 마다 우선순위 부여되고, 우선순위 높은 task 가 먼저 수행됨

2. 실시간 스케쥴링 기법 
2.1 정적 실시간 스케쥴링 알고리즘(RM, Rate Monotonic)
- 고정 우선순위 선점형, 짧은 주기 태스트가 높은 우선순위, 마감시간과 주기가 일치
- 원리 : 태스크간 선행관계 없는 조건 만족시, 수행전에 주기 짧은 태스크에 높은 우선순위 부여  
- 단점 : 주기가 긴 태스크들의 우선수위가 낮아서 장시간 대기 

2.2 동적 실시간 스케쥴링 알고리즘(EDF, Earlist Deadline First)
- 동적 우선순위 선점형, 태스크 들의 우선순위가 시간흐름에 따라 동적 변함
- 원리 : 스케쥴링 순간마다 프로세스 검사, 마감시간이 가까운 태스크를 우선 스케쥴링함  
- 단점 : 태스크들의 수행시간, 마감시간 주기등을 정확히 예측하는 것이 현실적으로 어려움

RM 스케쥴링은 Soft(연성) RTOS 에 적합하고
EDF 스케줄링은 Hard(경성) RTOS 에 적합하다.
```

| -           | RM 스케줄링                | EDF 스케쥴링                   | 
|-------------|------------------------|----------------------------| 
| 정책          | 정적 우선순위 알고리즘           | 동적 우선순위 알고리즘               |
| 우선순위        | 수행주기가 짧은 프로세스          | 마감 시간이 짧은 프로세스             | 
| 오버헤드        | 스케쥴링 오버헤드 낮음           | 스케쥴링 오버헤드 높음 |
| CPU 이용율 | CPU 이용율 낮음 | CPU 이용율 높음 |




#### [31/134] Priority Inversion(우선순위 역전)
```aiignore

```

