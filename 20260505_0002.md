

#### [38/134] Thread / Multi-Thread
```aiignore
1. Lightwright Process, 쓰레드
- 하나의 프로세스에 포함되어 프로세스의 특성중 일부기능(실행부분)만을 수행하도록 된 Light Weight Process

2. 쓰레드 적용 효과
1) 자원공유
2) 동기화
3) 병렬성
4) 독립적 스케쥴링
5) 분할과 정복 : 프로세스에서 실행의 개념만을 분리
6) CPU 오버헤드 감소 

3. 쓰레드 종류
1) 사용자 스레드
2) 커널 스레드
```

#### [39/134] Hyper Threading 
```aiignore
1. 하이퍼 스레딩
- 물리상 실행장치 1개에 가상실행장치(virtual 또는 logical core) 두개를 할당해 성능을 높이는 기술
```


#### [40/134] Operating System
```aiignore

```



#### [41/134] Micro Kernel / Monolithic Kernel  
```aiignore
1. 경령화된 OS 의 핵심부분, 마이크로 커널
- 프로세스/메모리 관리등 핵심기능만을 커널에 구현, 나머지 기능은 독립적 서비스 모듈로 구현한 운영체제 구조

2. 모놀로틱 커널 
- 시스템 운영에 필요한 많은 서비스 루틴들을 포함한 구조를 가진 운영체제의 core 프로그램

3. 구성요소
3.1 마이크로 커널 : Win NT, Mac OS 등 
1) Process manager
2) Thread Manager
3) Memory Manager : 물리/캐시/가상 메모리 관리 
4) Commmunication Manager : IPC 매커니즘 제공
5) Supervisor : 하드/소프트웨어 인터럽트 및 예외관리 

3.2  모롤로틱 커널 : 마이크로 커널 구성요소 + 3 ex) Solaris, HP-UX, Linux
1) Device Manager : 하드웨어 제어, 자원할당, 상태관리, 스케쥴링
2) File System Manager : 파일시스템 생성, 자원할당, 상태관리, 스케쥴링
3) Network Manager : 네트워크 연결, 자원할당, 상태관리, 스케쥴링 
```

| - | 모놀리틱 커널 | 마이크로 커널 |
| - | - | - |
| 개념 | OS 주요기능을 커널에서 제공 | 기본기능 외에는 서버 프로세스 형태로 제공| 
| 크기 | 커널크기 비대화 | 커널크기 최소화 |
| 유연성 | 기기 종속적 | 유연성(선택권 부여) |
| 확장성 | H/W 종속적으로 인한 확장 곤란 | H/W 종속성 최소화로 확장 용이 |
| 용량 | 대용량 | 소용량 |
| 성능 | 자원 사용 효율 우수 | 메세지 전송 및 context switch 로 오버로드 |
| 컴포넌트 | 미포함 | 포함 |
| 사용성 | 사용자 투명성 제공으로 사용용이 | OS 지식 필요로 사용성 저하 |
|사례 | Solaris, HP-UX, Linux | Win-NT, MacOS | 


#### [42/134] resource allocation graph
```aiignore
- 교착상태 발견기법, 자원할당 그래프 
- 프로세스와 자원간의 관계를 정점과 간선을 이용한 그래프로 표현하여, 교착상태를 발견하는 기법 
```




#### [43/134] DeadLock(교착상태)
```aiignore
1. 교착상태
다중 프로그램 환경에서 두개 이상으 프로세스가 서로 상대방의 자원을 요구하면서 양쪽 모두 작업수행을 할 수 없는 대기상태 

2. 교착상태 발생원인
1) 상호배제 : 프로세스가 자원을 배타적으로 점유하여, 다른 프로세스가 그 자원을 사용할 수 없음
2) 점유와 대기 : 자원을 점유하고 있으면서 또 다른 자원을 요청하며 대기
3) 비선점 : 자원을 점유한 프로세스만이 해제 가능
4) 환형 대기 : 자원의 점유와 대기가 하나의 원형을 구성한 상태 

3. 교착상태 해결방안
1) 예방 : 상호배제를 제외한 나머지 교착상태 발생조건을 위배(부정)하는 방안  -> 점유자원 해제후 새 자원 요청
2) 회피 : 안전한 상태를 유지할 수 있는 요구만 수락  -> 은행가 알고리즘
3) 발견 : 시스템의 상태를 감시 알고리즘 통해 교착상태 검사 -> 자원할당 그래프 
4) 회복 : 데드락이 없어질 때까지 프로세스를 순차적으로 kill 하여 제거 -> 프로세스 kill, 자원선점
```


#### [44/134] Race Condition(경쟁조건)
```aiignore
1. 한정된 자원을 획득하려는 프로세스들의 경함현상, 경쟁조건
두개 이상의 명령어가 동시에 같은 기억장소를 access 할때 그들 사이의 경쟁에 의해 수행결과를 예측할 수 없는 상태 

2. 해결방안
1) 단일처리기 시스템 에서의 해결방안
- 세마포어 : 자원의 독점과 무한대기를 방지
- 상호배제 의 최소화 
- Critical Section access 를 통해, 한 프로세스가 한 자원을 오랜 시간 독점 못하게 함 
2) 다중 처리기 시스템 에서의 해결방안
- 단일처리기 해결방안 포함
- spinlock timeout : 락이 이미 잡혀 있으면 스레드가 잠들지 않고 CPU를 계속 점유하면서 반복 체크(spin), Spinlock timeout = 락을 너무 오래 기다린 상태
    - 리눅스에서는 spinlock stuck 또는 soft lockup 경고 발생 -> BUG: soft lockup - CPU#0 stuck for 22s!
    - fallback 또는 오류 처리

3. 경쟁조건 억제방법
- 임시파일을 만들지 않음(임시파일 생성시 경쟁조건 발생)
- Create 와 Open 구문 사용(파일이 존재하지 않는 경우만 파일 생성)
- 심볼릭 링크 사용 주의(비정상 링크 제거, 수퍼유저 권한 파일에는 링크 설정 못하게 통제)

4. 경젱조건 해결의 tradeoff
- 자원 무한 독점 방지 : 잦은 lock 과 release 반복으로 성능 저하 발생
- 획득 - lock - release -wait 등의 상호관계 고려, 적정선의 독점사용 및 강제종료 배합 필요 
```

#### [45/134] 은행가 알고리즘
```aiignore
1. 은행가 알고리즘
- 운영체제는 시스템의 안전 상태와 불안정상태를 판단하여, 자원할당시 교착상태를 방지하는 사전 모니터링 알고리즘

2. 은행가 알고리즘 전제조건
- 할당량 존재 : 할당할 자원량이 일정량 존재해야 함
- 최대 자원량 : 최대자원의 요구량을 미리 알아야 함 
- 유한반납 : 프로세스들은 유한한 시간안에 자원을 반납해야 함

3. 은행가 알고리즘 처리절차
1) 필요자원 검사
2) 여유자원 검사
3) 자원상태 예측
4) 상태검사
```


#### [46/134] DMA 1
```aiignore
1. CPU 의 도움없이 IO 장치가 직접 메모리에 접근하는 방식
- CPU 와는 상태, 제어정보만을 교환하고, CPU 의 도움없이 DMA 컨트롤러를 이용하여 데이터를 직접 메모리로 전송하는 입출력방식

2. 특징
1) CPU Utilization 향상
2) 멀티 프로세스 환경에서 유리
3) 프로세스 응답시간 향상(느린 IO 작업으로 다른작업 대기하지 않음)

3. 동작방식
1) CPU 에서 DMA 컨트롤러로 IO 요청
2) DMA 컨트롤러가 CPU 로 부터 제어권 획득
3) DMA 컨트롤러는 IO 인터페이스에 데이터 적재하도록 DMA Ack 전송 
4) IO 인터페이스는 데이터 전송
5) DMA 컨트롤러가 CPU 에 제어권 반환(결과 Interrupt 발생)

4. DMA 동작모드
1) Burst Transfer Mode
- DMA 인터페이스가 버스 사용권을 획득하면 데이터전송을 마칠때 까지 버스 사이클 독점, 블록단위 전송

2) Cycle Stealing Mode
- DMA 가 데이터 전송을 위해 프로세서의 1개의 메모리 Cycle 을 중지할 것을 요청. 워드단위 전송

3) Interleaved DMA 
- CPU 가 시스템 미사용시 DMA 가 버스를 사용

5. DMA 외 입출력방식
1) Programmed Driven IO
- CPU 상에서 실행되는 프로그램에 의해 입출력이 제어되는 방식
- CPU 는 입출력장치에게 명령송신후 동작완료시 까지 대기, 비효율적인 동작과 자원낭비

2) Interrupt Driven IO
- IO 인터페이스가 주변장치의 Status 를 체크하고, 인터럽트 신호 발행하여 CPU 에게 입출력 요구
- 주변장치에 명령송신후 주변장치로 부터 결과를 피드백 받을 때 까지 CPU 는 다른 작업 수행 가능

3) IO 채널 프로세서
- DMA 개념을 확장. IO 전용처리장치를 이용 독자적인 프로세서를 가지는 형태
- 하나의 입출력 Instruction 에 의하여, 여러개의 Block 을 입출력
```


#### [47/134] DMA 2
```aiignore
1. DMA 동작모드
1) Burst Transfer Mode(Burst Mode, Block Mode)
- DMA 인터페이스가 버스 사용권을 획득하면 데이터전송을 마칠때 까지 버스 사이클 독점, 블록단위 전송
- 여러개의 메모리 word 로 구성된 블록이 지속적으로 전송됨 
- 전송단위 : 블록

2) Cycle Stealing Mode(Word Mode, Cycle Stealing)
- DMA 가 데이터 전송을 위해 프로세서의 1개의 메모리 Cycle 을 중지할 것을 요청. 워드단위 전송
- 전송단위 : 워드
- CPU 의 한 메모리 사이클을 훔쳐서 수행한다고 하여 Cycle Stealing 이라 호칭
- 한번의 DMA 동작 안에 한 word 의 데이터를 전송하는 경우 
```


#### [48/134] SCSI RDMA(Remote Direct Memory Access) 
```aiignore
SCSI Device 를 통한 메모리 원격접근 SCSI RDMA
- SRP 프로토콜을 활용하여 저지연, 고성능 네트워크 토인을 지원하는 메모리 활용 기술 

iSCSI
- SCSI 명령을 IP 네트워크(TCP/IP) 위에서 전송하는 스토리지 프로토콜
- Application → Kernel → TCP/IP → NIC → Network

iSER = iSCSI의 TCP/IP 경로를 제거하고, RDMA로 직접 메모리 간 데이터 전송을 하는 고성능 버전
- Application → RDMA → NIC → Network

기반기술
1) TCP/IP
2) iSCSI Extension for RDMA 
3) SRP(SCSI RDMA Protocol)

구현기술
1) Infini-Band
2) 10GbE network
```


#### [49/134] Memory Mapped IO, Isolated Mapped IO
```aiignore
1. 입출력장치와 주소지정 방식
1) 입출력장치 
- 중앙처리장치 내에서 수행할 데이터를 주 기억장치로 입력하는 동작과, 수행한 결과를 출력매체로 이동시키는데 필요한 장치

2) 주소지정방식
- Memory Mapped IO : 주기억장치의 일부 주소를 입출력 장치에 할당하는 방법
- Isolated Mapped IO : 주기억장치와 입출력장치가 사용하는 주소공간의 접근/저장 방식을 완전히 분리하여 명령어를 사용하는 방식
```

| -      | Memory Mapped IO                              | Isolated Mapped IO                                |
|--------|-----------------------------------------------|---------------------------------------------------|
| 특징     | CPU 내부 로직이 덜 필요하고, 더 저렴하고 빠르고 쉬운 CPU 설계가 가능   | 어드레싱 능력이 제한된 CPU 를 사용할 시 메모리 주소영역 전체를 사용할 수 있음    | 
| 고려사항   | 메인메모리에 접근하는 것 보다 매핑한 장치에 접근하는 것이 느림           | IO 제어를 위해 IO 명령어들만 이용할 수 있으므로 프로그래밍이 불편           |
| 사용분야   | RISC 임베디으 프로세서 등에서 주로 사용                      | 범용성 넓어 입출력장치 추가/삭제가 자유로운 PC 환경에서 주로 사용            |
| 특성     | CPU 가 사용하는 다양한 명령어와 주소법을 사용할 수 있음             | 명령어의 길이를 짧게 할 수 있고, 명령어의 인출과 실행시간을 줄일 수 있음        |
| 명령사용방식 | 입출력문이 따로 없이 기억장치에 Read/Write                  | 독자적인 입출력문 사용                                      |
| 기억장치   | 이용효율이 낮음                                      | 이용효율이 높음                                          |
| HW     | 단순함                                           | 복잡함                                               |
| 메모리공간  | 메모리중 일부만 사용                                   | 전체메모리 공간 사용                                       |
| 비고     | 메모리 location 을 엑세스 하는 방법으로 엑세스 별도의 IO 명령어 불필요 | IO 를 위한 별도 명령어 필요.<br/>IO 명령어가 각 IO 디바이스를 지정하여 수행 |




#### [50/134] Cache Memory 
```aiignore
1. 장치간 속도차이 극복, 캐시 메모리
- 고속의 중앙처리 장치와 상대적 저속의 주기억 장치 사이의 속도차이를 보완하기 위하여, 데이터와 명령어를 일시적으로 저장하는 기억장소

2. Cache Memory 의 특징
1) locality : 블록단위의 메모리 참조 -> 시간, 공간
2) Mapping : 주기억장치와 캐시메모리간의 메모리 매핑 적용 -> 직접, 연관
3) Coherence : 병렬처리시 Local Cache 과 공유 메모리간 데이터 일관성 유지 -> 공유 Cache

3. 캐시 매모리 구성요소
1) Cache : 지역적, 저용량, 고속화의 특성을 가질수록 고성능
- L1 Cache : CPU 안애 내장
- L2 Cache : 속도가 L1 보다 느림

2) Block : Cache 와 Memory 간의 데이터 교환 단위의 Block 이 클수록 locality 가 늘어남

3) Memory Function
- 메모리의 크기보다 Cache 가 작기 때문에 메모리의 어느부분을 캐시에 매핑할지를 결정해 주는 함수

4) Replace Algoritism(LRU, FIFO, LFU, 임의, OPtimal)
- 캐시의 어느 블록을 교체할지에 관한 알고리즘 -> 가장 널리 쓰이는 방법은 LRU(Least Recently Used)

5) Write  Policy 
- Write Through, Write Back 
- 캐시 데이터를 어떻게 메모리에 쓸지에 관한 정책관리

4. Write Policy
1) Write Through(즉시 쓰기 방식)
- 캐시를 갱신할 때마다 주기억장치 동시에 갱신 -> 단순 일관성 보장, 트래픽양 많음, 쓰기시간 길어짐
2) Write Back(나중 쓰기 방식)
- 쓰기동작 최소화, 쓰기 시간 단축 -> 일관성 유지 어려움, 블록교체시 상태값 확인 필요
- 캐시에만 갱신하고 해당 데이터가 swap-out 될 때 주기억장치에 복사하는 방식
```


#### [51/134] Cache Memory(Locality)
```aiignore

```

