# 인공지능
# 20260330_0001



```
정형 데이터 : 형식이 정해진 데이터 

비정형 데이터 : 규칙성이 없다.

반정형 데이터 : 중간형태 데이터
ex) HTML 문서, 로그 데이터



x1, x2, x3 : 중요한 요소들
w1, s2, w3 : 각 요소들의 중요도인 가중치 

y = w1x1 + w2x2 + w3x3 + ... + wnxn + b



y = ax + b
f(x) = ax + b

머신러닝에서는 H 로 쓴다.
함수의 의미보다 수식이 가설(Hypothesis, 하이파씨씨스 ) 를 강조 하기 위해서다.
w 는 weight (가중치)
b 는 bais (편향)

H(x) = wx + b


어떤 현상을 선형적으로 표현할 수 있을때 linear regression(선형회귀) 라고 한다.

회귀는 “trend fitting” 또는 “mean modeling” 의미

선형회귀는 선형 방정식을 사용하여 데이터의 관계를 모델링하는 회귀 분석 방법이다.

가설 1차 함수(회귀선) 과 실제 데이터 사이의 차이가 손실(loss, cost) 이다.

오차(error) = H(x) - y
====> 부호가 있는 값(양수, 음수)

∑(H(x)−y)
error 를 다 더 하면 + 오차 - 오차가 상쇄됨 
오차가 큼에도 0 이 될 수 있다.
따라서 error 자체는 를 손실로 보는 것은 부적합 하다.

따러서, 제곱 오차를 사용한다.
loss=(H(x)−y)^2

오차의 제곱을 평균 낸 값 = MSE (Mean Squared Error) 라고 한다.

==> “이 모델은 평균적으로 이 정도 수준으로 틀린다”

MSE = 4
평균적으로 오차 제곱이 4
RMSE = √4 = 2
실제 단위 기준으로 보면
평균적으로 약 2 정도 틀린다

MSE = “모델이 실제값 주변에서 얼마나 퍼져 있는지”
MSE = "모델이 평균적으로 얼마나 크게 틀리는지를 나타내는 값"



경사하강법
w 를 이동하면서 cost 가 0 인 지점을 찾아가는 방법
이 과정을 자동으로 찾아가는 것을 "기계가 학습한다" 고 표현함
학습이 끝났다는 것은 cost 가 0 에 가까운 w 에 도착했다는 의미다.

w는 x축
cost 를 y축으로 하는 2차 함수
제곱처리를 함에 따라 경사하강법을 적용할 수 있는 2차함수가 된다.


w와 b 를 모두 적용히면 3차원 바가지 모양으로 표현된다.




이진분류(Binary Classification)
- 둘중 하나를 선택하는 문제 


다중분류(Multicass Classification, Multilabel Classification)
```


```aiignore
뉴런 = 수상돌기 + 신경세포체 + 축삭

수상돌기 : 자극신호 입력받아 신경세포로 보냄
축삭 : 신경세포가 내보내는 자극을 다른 신경세포로 전달 
 
입력신호 -> 수상돌기 -> 신경세포체 -> 축삭 -> 축삭말단 -> 출력신호

신경세포로 전달되는 자극값들의 합이 일정 기준을 넘어서면 다음세로포 전달되고, 아니면 그냥 소멸된다.


여러 입력신호 값들을 가중치 부여해서 활성화 함수에서 일정치 이상이면 출력을 활성화 




페셉트론 : 뉴런의 작동방식을 인위적으로 모방한 모듈 


다층 퍼셉트론 
- 한층을 거쳐 출력된 값이 다른 층의 입력값으로 다시 들어가는 것은 수식 안으로 수식이 다시 들어간다는 것을 의미한다.
- 함수안에 함수가 재귀적으로 포함되는 것이다.
- 이렇게되면 가설식에는 차원이 늘어나는 효과가 생긴다.



DNN(Deep Neural Network)
- 퍼셉트론을 다층으로 구성한 딥러닝의 기본형 모델
- 입력층, 하나 이상의 은닉층, 출력층으로 구성된다.
- 일반적으로 최소 3개 이상의 층으로 구성된 경우를 딥러닝 이라고 부른다.



CNN(Convolutional Neural Network, 컨벌루셔널, 합성곱)
- 이미지 인식의 대표모델
- 주변화소의 특성까지 함께 학습한다.
- 학습해야할 가중치를 "가로세로 넓이를 갖는 하나의 작은 면 형태의 세트"로 구성한다.
    - 이 가중치의 세트, 가중치 집합을 CNN 에서는 필터라 부른다.
    - 이 필터가 주어진 이미지를 스캐닝 해서 각 화소가 갖는 특성을 주변 화소들의 특성과 함께 학습한다.
- 필터의 학습 수준이 정확도를 좌우한다.



RNN(Recurrent Neural Network)
- 앞뒤 데이터 사이에 연관성이 있는 데이터, 
    - 즉 시간적 또는 공간적 연속성을 갖는 데이터와 같이 
        - "이전 데이터가 다음데이터에 영향을 주는" 데이터 세트에 적합한 모델이다.
ex) 일별 주식 시세 데이터 

"동해물과" 입력하면 다음에는 "백두산이" 가 입력될 가능성이 크다.
이와같이 앞뒤 데이터 사이에 연관성이 있는 데이터 세트를 학습해, 
그 연관성을 고려한 예측을 수행하는 모델이 RNN 이다.

-- RNN 활용
검색창 자동완성 기능
스마트폰의 음성 인식을 통한 문장 입력기능
각종 언어의 통번역 
기계와 사람이 채팅하는 챗봇
영상의 자막 자동 입력 기능등

```


```aiignore
기계학습의 3대 유형
- 지도학습(Supervised Learning)
    - 레이블, 즉 지도정보가 붙은 데이터들을 제공하고, 
        - 이 데이터들을 사용해 답을 확인하면서 예측모델의 가중치를 수정해 가는 방식
            - 선형회귀, 이진분류, 다중분류, CNN, RNN 

- 비지도학습(Unsupervised Learning)
    - K-means 
        - 주어진 데이터들을 분석해 특성이 유사한 데이터들을 자동으로 그룹화 하는 알고리즘 
        - 고객유형분류, 데이터의 이상치 탐지, 문서 분류 등에 사용 
    - GAN(Generative Adversarial Network, 에드버세리얼, 대립적인)
        - 진짜와 유사한 가짜 데이터를 생성하는 생성자(generator)
        - 진짜와 가짜를 판별하는 역할을 하는 감별자(discriminator, 디스크리머네이터, 판별자)
        - 생성자, 감별자가 서로 경쟁하도록 하는 학습방법을 이용한다. 생성자, 감별자 둘다 AI 모델이다.
        - 감별자가 맞히는 확률이 50% 이면 학습은 마무리 된다.
        - ex) 유명인 사진을 바탕으로 GAN 이 가상 인물의 사진을 만든다.
        
- 강화학습(Reinforcement Learning)
    - 학습 데이터 없이 시행착오를 통해 컴퓨터가 학습하는 기법      
```


```aiignore
GPT(Generative Pre-trained Transformer) 는 자연스러운 문장 생성을 목적으로, 
대규모 컴퓨터 자원을 투입하여 만든 일종의 자연어 처리 소프트웨어다.

이와 같은 인공지능 모델을 대규모 언어 모델(Large Language Model, LLM) 이라 한다.

자연어 처리(Natural Langauge Processing, NLP)
토큰 : 형태소로 잘개 쪼갠 하나하나
인덱싱 : 토큰들에 일련번호를 매겨주는 작업 
어휘목록(vocabulary) : 토큰들을 나열한 집합

단어를 각각의 특성이 담긴 숫차 체계로 변환하는 작업을 워드 임베딩 이라 한다.

자연스러운 문장 생성을 위해 만들어진 머신러닝 모델을 언어모델(Language Model, LM) 이라 한다.

워드임베딩이 단어를 표현하는 방법
- 다차원 공간상의 좌표값으로 단어를 배치한다.
    - 단어의 특성이 요소 3개로 표현된 경우에는 이것을 3차원 가상공간의 형태로 표현할 수 있다.(Vector)
    - 임베딩(embedding) 은 목적물을 어떤 공간에 배치한다는 것이다.


전이학습(Transfer Learning)
- 이미 학습이 완료된 머신러닝 모델이 새로운 모델에게 자신의 가중치 설정값을 전수해 주는 것을 말한다.

```
