


#### [21/125] MPTCP(MultiPath TCP)
```aiignore

```


#### [22/125] STCP
```aiignore

```
#### [23/125] RTSP(Real Time Streaming Protocol)
```aiignore

```

#### [24/125] 라우팅 프로토콜
```aiignore

```

#### [25/125] RIP(Routing Information Protocol)
```aiignore

```

#### [26/125] OSPF(Open Shortest Path First)
```aiignore

```

#### [27/125] BGP(Border Gateway Protocol)
```aiignore

```

#### [28/125] IP Multicast
```aiignore

```

#### [29/125] CIDR(Classless Inter-Domain Routing)
```aiignore
1. CIDR 
1) 클래스 없는 도메인간 라우팅 기법으로 1993년 도입되기 시작한 IUP 주소 할당 기법(네트워크 클래스를 대체)
2) 도메안간의 라우팅에 사용되는 인터넷 주소를 원래의 IP 주소 클래스 체계를 쓰는 것보다 더욱 융통성 있도록 할당/지정하는 방식

2. CIDR 표기법
- [기존 표기법] IP:192.168.1.17, 서브넷마스크:255.255.255.0
- [CIDR 표기법] 192.168.1.17/24
```

#### [30/125] Supernetting
```aiignore

```



#### [31/125] Subnetting
```aiignore

```

#### [32/125] DNS
```aiignore

```

#### [33/125] VPN
```aiignore

```

#### [34/125] Tunneling
```aiignore

```

#### [35/125] TLS 1.3
```aiignore
- TLS 1.2 의 보안성, 속도향상, 암호화 성능을 개선하기 위해 설계된 최신 네트워크 보안 프로토콜  
```

#### [36/125] NAT
```aiignore
1. NAT
- 외부 네트워크에 알려진 것과 다른 IP 주소를 사용하는 내부 네트워크에서 IP 주소를 반환

2. NAT 특징
- 공인 IP 주소의 절약, 고유 사설망의 보호

3. NAT 의 종류
1) static NAT(1:1)
2) dynamic NAT(m:n)
3) PAT(Port Address Translation) : 여러 내부 IP 가 하나의 공인 IP 를 공유할 수 있도록 TCP/UDP 의 포트번호까지 변환하는 방식(공유기)
4) LSNAT(Load Sharing Network Address Translation) : 하나의 외부 IP 주소에 복수개의 내부 IP 주소 매핑
```


#### [37/125] VLAN(Virtual Locall Area Network)
```aiignore
가상 기능을 가진 LAN 스위치를 사용해서 물리적인 배선에 구애받지 않고 broadcast packet 이 전달되는 범위를 임의로 나눈 가상의 LAN
```


#### [38/125] VXLAN(Virtual Extensible LAN)
```aiignore

```

#### [39/125] MPLS(Multiprotocol Label Switching)
```aiignore

```


#### [40/125] DHCP(Dynamic Host Configuration Protocol)
```aiignore
1. DHCP
- 네트워크 관리자가 조직의 중앙에서 일정한 시간동안 유효하도록 하는 임대개념의 IP 주소를 할당하고 관리하는 프로토콜

2. DHCP 동작원리(DORA)
1) Discover : Cient 의 DHCP 서버를 찾는 broadcast
2) Offer : DHCP 서버가 IP Address 를 하나 담아서 broadcast
3) Request : client 는 할당받은 IP 와 보내준 서버 IP 를 packet 으로 만들어 network 내 DHCP 서버에게 보냄
4) Ack : DHCP 는 자신의 IP 선택되면 Ack(IP 임대기간, DNS, GW 등의 정보 packet)을 vroadcast
```

#### [41/125] IPv6
```aiignore

```


#### [42/125] QoS(Quality of Service) 1
```aiignore
1. QoS
- 네트워크망의 대역폭을 효율적으로 제어하여 네트워크 트래픽을 정책별 활용. 인터넷 종단간 서비스 품질을 향상시키는 기술
```

#### [43/125] QoS(Quality of Service) 2
```aiignore

```

#### [44/125] 회선교환 / 패킷교환
```aiignore

```

#### [45/125] CSMA/CD
```aiignore

```

#### [46/125] Multiplexing(TDM, FDM, CDM)
```aiignore

```

#### [47/125] TDMA, FDMA, CDMA
```aiignore

```


#### [48/125] 디지털 계위(Digital Hierarchy)
```aiignore

```

#### [49/125] DWDM(Dense Wavelength Division Mulplexing)
```aiignore
1. DWDM
- 하나의 광케이블에 수십, 수백개의 광파장을 실어 전송하여 전송용량과 채널의 수를 늘여 전송거리와 용량, 속도를 확보한 기술
```


#### [50/125] OFDM
```aiignore

```

#### [51/125] 변조(PCM)
```aiignore
1. PCM
- 아날로그 신호를 시간 간격으로 샘플링하고, 이를 비트열의 디지털 신호로 변조하여 전송하는 통신기술 

2. PCM 동작원리
1) 표본화(sampling) : 연속적으로 변화하는 값을 일정한 시간 간격으로 나누어 표본을 추출하는 것
2) 양자화(quantization) : 표본화에 의해 얻은 PAM 신호를 디지털화 하기 ㅜ이해 딘폭축으로 이산값을 갖도록 처리하는 것을 양자화 
3) 부호화(encoding) : 양자화된 신호를 0과 1의 이진비트로 표현하는 과정 
```

#### [52/125] 델타변조
```aiignore
 
```


#### [53/125] 네트워크 부호화
```aiignore
 
```

#### [54/125] 네트워크 압축기법
```aiignore
 
```

#### [55/125] 허프만 코딩
```aiignore
 
```
#### [56/125] 맨체스터 코딩
```aiignore
 
```

#### [57/125] IEEE 802.3
```aiignore
1. 유선네트워킹을 위한 이더넷 기술 표준, IEEE 802.3
- LAN, MAN, WAN 에서 사용되는 네트워크 기술 표준
```

#### [58/125] WiFi(IEEE 802.11xx)
```aiignore
1. WiFi
- IEEE 802.11x 표준을 기반으로 기존 LAN 환경을 바탕으로 무선 주파수를 사용하여 일정공간 내에서 네트워크에 접속할 수 있는 기술 

2. WiFi 규모에 따른 구성유형
1) BSS(Basic Service Set)
2) ESS(Extended Service Set)
3) Ad hoc 

3. WLAN 보안
1) SSID
2) MAC 
3) WEP : WEP 의 주요목적은 접근제어, 프라이버시 기능을 제공
4) WPA(WIFI Protected Access) : WEP 보안구조 보완
```

#### [59/125] IEEE 802.11ac 
```aiignore

```

#### [60/125] WiGig
```aiignore

```

#### [61/125] IEEE 802.11af
```aiignore

```

#### [62/125] IEEE 802.11ax
```aiignore

```

#### [63/125] Wi-Fi 6E
```aiignore

```

#### [64/125] IEEE 802.11be / Wi-Fi 7
```aiignore

```

#### [65/125] IEEE 802.11bn 
```aiignore

```


#### [66/125] LiFi(Light Fidelity)
```aiignore
1. Li-Fi(Light Fidelity)
- 발광 다이오드(LED)를 활용하여 적외선에서 근자외선까지 스펙트럼의 빛을 이용한 5세대 이동통신 기술
```

#### [67/125] 모바일 FSO(Free Space Optics) 통신
```aiignore
1. 모바일 기반의 자유 공간 광통신, 모바일 FSO(Free Space Optics) 통신
- 자유공간에서 전파되는 빛(레이저)을 사용하여, 모바일 기반에서 통신 또는 컴퓨터 네트워킹을 위해 데이터를 전송하는 장거리 광 통신기술
```


#### [68/125] IEEE 802.11i(와이파이 보안)
```aiignore
1. IEEE 802.11i
운영되는 IEEE 802.11 기반의 무선 네트워크의 보안 메커니즘을 규정한 표준
```

#### [69/125] MIMO(Multi Input Multi Output)
```aiignore
1. MIMO(Multi Input Multi Output)
- 무선 통신의 용량을 높이기 위한 스마트 안테나 기술
```

#### [69/125] MIMO(Multi Input Multi Output)
```aiignore
1. MIMO(Multi Input Multi Output)
- 무선 통신의 용량을 높이기 위한 스마트 안테나 기술
```

#### [70/125] 스마트 안테나
```aiignore
1. 스마트 안테나
- 배열 안테나의 지향성 빔을 이용하여 간섭신호 및 잡음을 제거하여 통신 품질을 확보하는 기술
```


#### [71/125] WPAN(Wireless Personal Area Network)
```aiignore
1. WPAN(IEEE 802.15)
- 10M 이내의 짧은 거리의 통신 기기, 디지털 장치를 식별, 기기 사이의 데이터 전송과 제어가 가능한 ad-hoc 기반의 무선 네트워크 기술
```
