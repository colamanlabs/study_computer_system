
#### [119/134] CUDA
```aiignore
1. GPU 를 이용한 병렬 컴퓨팅 아키텍처, CUDA
- nVidia 에서 개발한 GPU 의 병렬처리기능을 활용하여, 범용적인 프로그램이 가능하도록 제공하여 주는 통합개발 환경 

2. CUDA 등장배경
1) 컴퓨팅의 진화방향 : CPU 의 중앙처리 개념에서 CPU 와 GPU 의 코프로세싱 개념으로 진화
2) GPU 활용을 위한 OpenGL 과 DirectX 활용의 어려움

3. 장점 및 단점
# 장점
1) 높은 연산처리능력
2) 병렬 프로그램의 확장성
3) 저렴한 가격
4) 고성능 서버보다 편리한 설치 및 유지

# 단점
- CUDA 프로그래밍에서 PC 와 그래픽 사이에 데이터 복사 과정

4. CUDA GPU 아키텍처 및 구성요소(SM/SP)
1) CUDA library
2) CUDA runtime
3) CUDA driver
4) GPU

5. CUDA 처리과정
1) 처리할 데이터를 MM -> GPU 메모리, 그래픽카드 공간 할당
2) PC의 입력데이터를 그래픽카드로 복사
3) GPU 성능일 이용하여 각 core 에서 병렬처리
4) 처리된 결과를 그래픽 카드 메모리에서 PC 메모리로 복사

6. 활용영역과 불가능 영역
1) 활용가능영역
- 컴퓨터 (물리연산, 그래픽처리, 비디오, 오디오), 생명공학(유전자, 의학 이미징), GIS 활용, 금융, high bandwidth

2) 불가능영역
- 병령 자체가 잘 안되는 알고리즘(static variable 및 if 알고리즘 종속이 심한경우), RTOS(embedded), cloud computing
```



#### [120/134] GPGPU
```aiignore
1. GPGPU(General Purpose GPU)
- GPU 를 그래픽 연산 뿐만 아니라 일반 컴퓨팅 영역에도 활용하고자 하는 컴퓨팅 체계

2. GPGPU 기반 컴퓨터 플랫폼 종류
1) CUDA(Compute Unified Device Architecture)
2) OpenCL(Open Computing Language)
3) C+++ AMP : Visual Studio 에 추가된 GPU 를 이용한 C++ 코드 실행 속도 향상목적 언어
4) OpenACC : CUDA 기반 병렬 프로그래밍의 어려움을 개선하여, CUDA 를 좀 더 추상화시킨 프로그래밍 모델
```


#### [121/134] 멀티 GPU
```aiignore
1. GPU 를 병렬로 연결하여 그래픽 렌더링 및 연산 가속화, 멀티 GPU
- 단일 시스템에 2개 이상의 GPU 를 장착하여, 그래픽 처리나 병렬 연산 작업을 분담/가속화 하는 하드웨어 및 소프트웨어 기술

2. 멀티 GPU 구현 매커니즘
1) GPU
2) 인터커넥트
3) 워크로드 분래
4) 데이터 동기화

3. 멀티 GPU 구현을 위한 필수기술
1) nvidia nvlink, amd infinity fabric
2) CUDA
3) Data parallelism
4) Model Parallelism
5) GPU PCIe
```




#### [122/134] TPU
```aiignore
1. tensorflow 믜 머신러닝 성능읠 최적화 하기 위한 주문형 칩셋 TPU(Tensor Processing Unit)

2. TPU 의 기술적 특징
1) 병렬처리기능
2) 연산기능
3) 지원서비스 : 구글 오픈소스 텐서플로우 지원
4) 생산방식 : ASIC(Application Specific Integrated Circuit)
5) 명령어 구조 : CISC 기반
6) 성능 : GPU/CPU 조합보다 15 ~ 30배 가량 성능이 높다고 함 
7) 연산성능 : 45 TFLOPS 칩 4개를 얹어 180 TFLOPS 성능 을 내는 모듈로 만들어짐
8) 지원대역폭 : 모둘 하나당 64GB 메모리 대역폭 지원
```


#### [123/134] 신경망 처리장치(NPU, Neural Processing Unit)
```aiignore
1. 인공지능 연산 가속화에 최적화된 전문 프로세서, NPU
- 딥러닝/신경망 모델의 추론, 학습연산을 고속/저전력으로 실행하기 위해 특화 설계된 반도체 프로세서

2. 신경망 처리장치 NPU 의 설계기술
1) 병렬 MAC 어레이 : 딥러닝 핵심인 행렬연산을 고속 병렬 처리  
2) 온칩 메모리  : 가중치, 활성화값등 데이터를 저장하는 고속 SRAM (고속내부버퍼)
3) 데이터 흐름 아키텍처 : 연산효율 극대화를 위한 데이터 처리순서 및 이동경로 제어 
4) 호스트 인터페이스 : CPU, 시스템 메모리와 명령 및 데이터를 주고받는 통로

3. 신경망 처리장치 NPU 의 구현기술
1) Systolic Array : 구글 TPU 등에서 사용하는 데이터가 물결처럼 흐르며 병렬 처리되는 고효울 아키텍처
2) Quantization(경량화) : FP32(실수) 연산을 INT8(정수)등으로 변환하여 속도를 향상시키고 전력소모를 감소
3) AI Compiler/Runtime : TensorFlow, PyTorch 등 AI 모델을 특정 NPU 에서 실행가능한 기계어로 변환/최적화
4) On-Device AI : 클라우드 연결 없이 기기(edge) 자체에서 NPU 를 활용해 AI 추론을 수행하는 기술
```


#### [124/134] Application Processor 
```aiignore
1. Application Processor
- 모바일기기에 적용되는 OS 및 각종 기능등을 구동시킬 수 있는 기능 모두를 하나의 칩으로 모아 놓은 SOC(System On Chip)
```


#### [125/134] Smart Card 
```aiignore
1. Smart Card
- 마이크로 프로세서와 메모리로 구성된 IC 칩이 내장되어 이어 다양한 정보를 축적하여 활용할 수 있는 정보화 카드

2. Smart Carad 의 특징
- 프로세스 장착 자체연산이 가능한 프로세스 내장
- 내부전원 없이 외부전원 공급방식 
- 탁월한 보안성 불법복제장치 및 안전한 데이터 보관이 가능 / 암호화 처리장치
- 다목적성 one card multi use 의 다목적성 (높은 휴대성)

3. smart card 의 분류
- 접촉식 : 전원을 판독기에 물리적 접촉에 의해 공급하는 방식으로 고도의 보안을 요구하는 시스템에 적합 
- 비접촉식 : 판독기 접촉없이 일정거리에서 동작하는 방식, 안테나 정전 유도방식, 트랜잭션 완료시간이 짧음, 수백 바이트의 짧은 데이터 
```


#### [126/134] USIM(Universal Subscriber Identify Module)

```aiignore
1. USIM(Universal Subscriber Identify Module)
- 가입자 식별 모듈을 구현한 IC 카드 (GSM 의 SIM 이 확장된 표준)

2. USIM 특징
1) 네트워크 접속 및 가입자 인증용 애플리케이션으로 통신용 스마트카드인 UICC 에 탑재
2) USIM 애플리케이션은 가입자 정보, 네트워크 정보, 인증정보 등의 중요정보와 텍스트 메세제, 이메일, 폰북, 개인부가서비스 정보저장

3. USIM 기능
1) 가입자 정보 판독
2) 인증 벡터 생성
3) 네트워크 인증
4) 인증 응답값 생성
5) USIM 카드 인증   
```




#### [127/134] SoC(System on chip)

```aiignore
1. SoC
- 여러기능을 가진 시스템(회로)를 하나의 칩에 집적하는 기술 집약적 반도체 기술

2. SoC 특징
1) 저전력
2) 이동성
3) 고성능
4) 고비용
5) 낮은 유연성
6) 표준형 반도체, ASIC(주문반도체)
```



#### [128/134] SiP(System in Package) / SoB(System on Board)

```aiignore
1. SiP : 서로 다른 부품이나 IC 를 하나의 패키지로 통합하는 방식을 통해 시스템 레벨의 고집적 IC 를 실현하는 방식

2. SiP 특징
1) 다품종 소량생산
2) 보안문제 해소 : IP 거래등에서 우려되는 보안문제
3) 소형화
4) 설계의 단순화 
```



#### [129/134] FPGA(Field Programmable Gate Array)

```aiignore
1. FPGA
- 약 100 ~ 1000 개의 마이크로 셀로 구성되어, SRAM 과 플래시 메모리 조합하여 만든 집적도 높은 프로그래머블 반도체 

# FPGA
- 프로그램 저장소 : 플래시 메모리를 외부에 두고, 내부에는 휘발성 메모리인 S램을 고밀도로 나열시킴
- 프로그래밍 방법 : CLB(Configurable Logic Block)을 구조적으로 연결시키고 모듈을 조합하여 의도하는 기능구현
- 프로그래밍 특징 : 휘발성 : 전원이 제거되면 회로구성이 초기화됨
- 배선정보 : 기동시 배선정보 다운로드 필요    
```



#### [130/134] PIM(Processing in Memory)
```aiignore
1. 폰노이만 아키텍처의 병목, 대기시간 개선을 위한 PIM
- 프로세서와 RAM 을 단일칩에 통합하여 메모리 내에서 계산 및 처리를 수행할 수 있도록 한 지능형 반도체     
```


#### [131/134] 가산기(adder), 반가산기, 전가산기
```aiignore
1. 가산기 : 덧셈 연산을 수행하는 노리회로이며 디지털 회로, 조합회로의 일정

2. 가산기의 종류
- 반가산기 : 이진수의 한자리수를 연산하고, 자리올림수는 자리올림수 출력(carry out)에 따라 출력
- 전가산기 : 이진수의 한자리수를 연산하고, 하위의 자리올림수 입력을 포함하여 출력, 임의의 자리수의 이진수 덧셈이 가능     
```


#### [132/134] JTAG(Joint Test Action Group)
```aiignore
1. 하드웨어의 테스트 연결상태를 체크하는 JTAG
- 디지털 회로에서 특정 노드의 디지털 입출력을 위해 Boundary-Cell 을 만들어 직렬통신방식으로 출력 데이터를 전송하거나 입력데이터를 수신하는 방식     
```



#### [133/134] 3 상태 버퍼(Tri-State Buffer)
```aiignore
1. Control Input 에 따른 출력신호 제어방식, 3-상태 버퍼(Tri-State Buffer)
- 별도의 Control Input 에 따라 출력신호를 3가지로 제어할 수 있도록 설계된 디지털 버퍼의 유형      
```



#### [134/134] HBM(High Bandwidth Memory)
```aiignore
1. 3D 동기식 동적 랜덤 액세스 메모리 HBM

2. 특징
1) 3D 스태킹 기술기반, TSV(실리콘 관통 비아) 수직채널을 통해 많은 수의 메모리칩을 더 작은 공간에 집약
2) 메모리와 프로세서 사이에 데이터가 이동해야 하는 거리가 주어듬

3. HBM 사영별 Spec
1) HBM
2) HBM2/HBM2E
3) HBM3
4) HBM 3E
5) HBM-PIM : PIM(Processing in memory)을 탑재한 HBM, AI 엔진, 병렬처리향상, 에러지절감(70%)

4. HBM 구성요소
# 3d stacked dram
1) HBM DRAM Die
2) Logic die
3) TSV : 미세전극으로 수직관통하여 연결

# processor unit
1) CPU, GPU, SOC
2) PHY : 물리계층 I/F, Logic Die Process 간 연결

# ChipSet
1) CPU-HBM Controller : 다이간 연결하는 칩셋
2) Package substrate : silicon interposer 연결기판      
```



