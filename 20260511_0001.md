


#### [1/125] OSI 7 Layer(ISO 7498)
```aiignore
     
```



#### [2/125] 계층별 프로토콜 
```aiignore
     
```


#### [3/125] TCP/IP
```aiignore
     
```


#### [4/125] UDP
```aiignore
     
```

#### [4/125] 계층별 장비 
```aiignore
L2 스위치
L3 스위치
L4 스위치
L7 스위치      
```


#### [5/125] ARP(Address Resolution Protocol) / RARP(Reverse Address Resolution Protocol)
```aiignore

```



#### [6/125] SNMP(Simple Network Management Protocol)
```aiignore
1. SNMP
- 네트워크 장비의 상태를 관리 감시하기 위한 목적으로 UDP 상에 정의된 응용 계층 프로토콜 
- 초기철학
    - 아주 가볍고 단순해야 한다.
    - 초기 네트워크 장비들은 H/W 사양 낮고, 네트워크 느렸음
    
2. SNMP 의 구성요소
1) 관리시스템(manage)
2) 관리대상(agent)
3) MIB(management information base) : 피 관리대상장비의 관리되어질 요소에 대한 정보를 저장한 db

4. MIB 의 5가지 기능분야
- 구성관리
- 성능관리
- 고장관리
- 보안관리
- 계정관리 
    
5. SNMP V2/V3
- V2 : access contorl 기능추가
- V3 : 보안서비스(인증, 암호화 접근통제 이용) 추가    
```


#### [7/125] NOS(Network Operating System)
```aiignore
1. NOS(Network Operating System)
- 컴퓨터와 장치, LAN 에 연결하기 위한 기능과, 네트워크 리소스를 관리하기 위한 특수기능이 포함된 운영체제
- Cisco IOS, Junos OS 등
- 라우터, 스위치, 방화벽에 설치      

2. NOS 의 기능
- 자원관리
- 접근제어
- 제공기능 : 스위칭, 라우팅, 방화벽등
- 구현표준 : OSI 7 Layer 상위 5계층은 NOS 토대로 제공
```


#### [8/125] NMS
```aiignore
1. NMS
- 네트워크 상의 모든 장비들을 중앙감시체제로 구축한 네트워크 관리 시스템

2. NMS 기능
- 계정관리
- 구성관리
- 성능관리
- 장애관리
- 보안관리
```



#### [9/125] L4 스위치(구동방식)
```aiignore
L4 스위치 서비스 제어 방법
1. 제어항목
- IP 제어, Port 제어, Load Balance(세션 관리), MAC 제어

2. Load Balance 방식
1) Round Robin
2) Lease Connection
3) Response TIme
4) Hash
5) Minumum Missis : Hash 기법과 유사하지만 특정서버 다운시 해시값으로 재할당
6) Bandwidth based loadbalancing : 서버들과의 대역폭을 고려하여 분산
```


#### [10/125] TCP/IP(3 way vs 4 way handshake)
```aiignore
1. 3way handshake
- [C->S] SYN    -> SEQ는 클라이언트 임의값
- [C<-S] SYN+ACK    -> SEQ 는 서버 임의값, ACK 는 요청 SEQ+1
- [C->S] ACK -> SEQ 는 클라이언트 seq+1, ACK 는 서버 SEQ+1

2. 4way handshake
- [C->S] FIN 
- [C<-S] ACK 
- [C<-S] FIN
- [C->S] ACK
```



#### [12/125] TCP 혼잡제어
```aiignore
1. 데이터 전송 신뢰성 보장, TCP 혼잡제어
- 네트워크로 유입되는 사용자 트래픽 양이 네트워크 용량을 초과하지 않도록 유지시키는 메커니즘

2. 혼잡제어 매커니즘
1) 네트워크 도움을 통한 방식
- 혼잡 발생시 네트워크 내의 라우터에서 감지후(버퍼사용량을 통함 감지) 송신/수신 호스트에게 알림
2) 종단(end-to-end) 호스트간 방식 
- 송신 호스트가 전송한 데이터 에 대한 수신 호스트의 확인 응답 유무, 지연시 네트워크 상태 파악

3. TCP 혼잡제어
1) 느린 출발 : 초기 환잡 윈도우 크기 1로 전송, 미리 정해진 임계값에 도달할 때 까지 윈도우의 크기를 2배씩 증가
2) 혼잡 회피 : 윈도우의 크기가 임게값에 도달한 이후에 윈도우 크기를 1씩 증가

3. 손실복구
1) 빠른 재전송 : timeout 안기다리고 dup(충돌) ack 시 재전송(TCP Tahoe)
2) 빠른 회복 : 윈도우 크기(cwd)를 반으로 줄임, 1씩 선형으로 증가(TCP Reno)

cf) 윈도우
EST 상태에서 연속해서 보낼수 있는 데이터 총량

cf)
서버의 ACK 는 동적이고 고정된 정책이 없음
```



#### [13/125] TCP 혼잡제어
```aiignore
1. TCP 흐름제어, 슬리이딩 윈도우 
- 수신쪽에서 설정한 윈도우 크기만큼 송신측에서 확인응답(ACK) 없이 전송할 수 있게하여 흐름을 동적으로 조절하는 제어알고리즘
```


#### [14/125] TCP 오류제어
```aiignore
1. TCP 오류제어
- 네트워크에서 전송된 세그먼트가 손실, 훼손, 중복, 순서가 어긋나는 경우에 대하여, 처리와 복구를 수행하는 TCP/IP 신뢰성 확보 기법
```


#### [15/125] TCP 오류제어기법1
```aiignore
```

#### [16/125] TCP 오류제어기법2
```aiignore
```


#### [17/125] TCP 오류제어기법3
```aiignore
```

#### [18/125] 트래픽 폴리싱(Traffic Policing)
```aiignore
1. 네트워크 트래픽 전송율 제어를 위한 트래픽 폴리싱
- 네트워크 트래픽이 약속된 전송율(CIR)을 초과하면, 패킷을 drop 하거나 우선순위를 낮추어(marking) 제어하는 QoS 기술
```

#### [19/125] 트래픽 쉐이핑(Traffic Shaping)
```aiignore
1. 네트워크 트래픽의 평탄화 제어를 위한 QoS 기술, 트래픽 쉐이핑
- 특정 트래픽의 전송속도 제어(지연)하여, 미리 정의된 프로파일에 맞게 트래픽 흐름을 조절하는 네트워크 관리 기술
```

#### [20/125] 해밍거리(Hamming Distnace)
```aiignore

```
