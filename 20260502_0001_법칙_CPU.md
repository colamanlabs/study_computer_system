###
```aiignore
2026.05.02
```



#### 1. 법칙 
```
키워드 
# 무어의 법칙
# 멧칼프의 법칙
# 암달의 법칙 
# 파레토의 법칙 
# 롱 테일 법칙 
# 구스타프슨의 법칙

# 무어의 법칙 
고든무어는 인텔 창립자임 
일정기간(1~3년) 마다 반도체 집적수는 2배로 증가하고, 성능도 증가한다.

# 멧칼프의 법칙 
메칼프는 이더넷 발명자, 3COM 설립자임
네트워크 효과는 네트워크 사용자 수에 비례한다.
n 개의 노드인 경우 n(n-1)/2 로 계산 
- 자기가 자신에게 요청하는 것을 빼야하고
- 연결자체를 말하는 것이므로 방향이 없다. 그래서 /2 하는 것임 

# 암달의 법칙
처리를 병렬화 해도, 더이상 성능 향상이 불가한 한계가 존재함 
- 예를 들어, CPU 코어 수가 늘어나도, 코어 간 통신이 늘어나면 성능 향상이 불가능함

# 파레토의 법칙
파레토는 이탈리아 경제학자
상위 20% 가 전체 생산의 80% 를 해난 다는 80:20 법칙  
20% 의 인구가 80% 의 땅을 소유하는 현상을 발표함
 
# 롱 테일 법칙
보통 사건이 많이 일어나는 소수의 영역에 집중하지만,
롱테일은 나머지 발생빈도는 0 에 가깝지만, 축의 대부분을 차지하는 것을 말함 
롱테일 용어 자체는 크게 수익이 나지 않는 상품군, 대상을 말함
그러나, 웹 (웹 2.0) 에서는 소비자들이 정보를 적극 공유함 
개미고객 80% 에서 나오는 매출이, 전통고객 20% 보다 더 큰 현상

# 구스타프슨의 법칙
대용량 데이터 처리는 효과적으로 병렬화 할 수 있다는 법칙 
암달의 법칙의 반대개념
```


#### 2. 컴퓨터 구조 및 적용기술 
##### 2.1 CPU
```
키워드
# 파이프라이닝, 수퍼 파이프라이닝
# 수퍼 스칼라
# RISC
# CISC
# EPIC


# 파이프라이닝  
명령을 처리하는 흐름은  
(메모리 혹은 캐시에서) 명령을 가져오고, 해석하고, 실행하고, 결과 저장(다른 명령이 사용가능하도록, 다른 레지스터에 저장) 흐름을 갖는다.
1개 클럭에 모든 단계를 처리한다면, 클럭 향상을 통해 성능향상이 어려울 것이다. 
클럭 마다 명령을 실행하는데, 
- 명령 처리에 시간이 들어가니, 단위시간단 명령 발행횟수(클럭수) 향상이 어렵고
- 각 명령은 세부 단계를 거치는데, 명령을 마칠 때까지, 세부 단계를 이미 거쳤거나, 현재 실행중인 이후 단계는 모듈이 쉬는 상황이 발생한다.
- 세부 공정이 4단계이면, 1개 공정만 실행중이니, 실제 효율은 25% 가 된다.
1개 클럭에 모든 처리를 다 한다면 비효율적이다.
해당 이슈 해소를 위해, 명령당 각 세부단계를 나누고 각 클럭에 각 단계별 처리로 하도록 하는 것이 파이프라이닝 이다.
각 단계별로 다음 세부 명령 처리를 받아둠으로서, 클럭중에 내부 공정을 모두 사용함으로서, 처리량을 높인다.
파이프라인은 각 stage 당 처리효율을읖 높이고, 
단계별 처리량을 줄임으로서, 
클럭 향상을 통해 성능개선을 기대할 수 있지만,
긴 파이프라인 중에 특정 이슈로 명령 실행을 다시해야 하는 경우(분기처리등)
앞서 진행된 파이프라인 단계를 손해보기 때문에, 
결국 여러클럭이 필요하게 되고,
클럭당 처리량에서 손해를 볼 수 있다. 
따라서, 파이프라인 단계를 과도하게 길게 하는 것 보다는(펜티엄4)
적당한 길이로 설계함으로서, 클럭당 처리량을 높이는 것이
CPU 성능 처리량 향상이 도움이 된다.

# 수퍼 파이프라인 
각 개념적 단계(stage) 를 분해해서, 다시 단계별로 구성하는 것이다.
실행1 -> 실행2 -> 실행3 -> 실행4 
파이프라인을 세분화 함으로서, 개별단계의 빠른 처리를 유도하고
이를 통해 클럭향상을 하는 것이 목적이다.

# 수퍼스칼라
파이프라인 단계에서 처리할 수 있는 실행모듈을 여러개 두는 것 

# RISC, CISC, EPIC
CISC : 가변길이의 다양한 명령어 제공, 컴파일러 구현이 상대적으로 용이하나, CPU 성능 향상 까다로움. 인텔 X86, X86_64 
RISC : 고정길이의 적은 명령어 제공, 컴파일러 구현이 상대적으로 까다로우나, CPU 성능 향상이 용이함. IBM POWER 아키텍처  
EPIC : 여려명령을 묶은 고정길이 구조, 컴파일러가 명시적으로 명령에 병렬내용을 기재, 인텔 아이테니엄
```
