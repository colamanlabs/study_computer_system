

#### [82/134] Boot Loader(부트로더)
```aiignore
1. 부트로더
- OS 구동시 커널이 올바르게 시동되기 위해 필요한 관련작업을 마무리 하고, 최종적으로 운영체제를 시동시키기 위한 프로그램

2. 부트로더의 종류
1) LILO(Linux Loader)
2) GRUB(GRand Unified Bootloader) : GNU 프로젝트 부트로더. 대부분의 운영체제의 커널을 불러올 수 있음 
3) BLOB(Boot Loader Object) : 부트로더 로딩객체
4) U-BOOT(Universal Bootloader) : 범용 부트로더
```





#### [83/134] FT(Fault Tolerant) 1
```aiignore

```