

#### [83/134] Boot Loader(부트로더)
```aiignore
1. 부트로더
- OS 구동시 커널이 올바르게 시동되기 위해 필요한 관련작업을 마무리 하고, 최종적으로 운영체제를 시동시키기 위한 프로그램

2. 부트로더의 종류
1) LILO(Linux Loader)
2) GRUB(GRand Unified Bootloader) : GNU 프로젝트 부트로더. 대부분의 운영체제의 커널을 불러올 수 있음 
3) BLOB(Boot Loader Object) : 부트로더 로딩객체
4) U-BOOT(Universal Bootloader) : 범용 부트로더
```





#### [84/134] FT(Fault Tolerant) 1
```aiignore
1. 시스템 안전보장, FT
- 하드웨어적, 소프트웨어적 고장이 발생하더라도 이에 영향을 받지 않고 설계상에 명시된 기능을 수행할 수 있는 능력

2. 결함해결단계
1) 결함 탐지
2) 결함 진단
3) 결함 통제
4) 결함 복구

3. 결함 허용을 위한 발생 영역별 대응 기법
# H/W 측면 기법
1) Duplication with Comparison
- 한 프로세스를 주 프로세스와 보조 프로세서에 중복 할당(중복성의 원리), 동일한 2개 모듈 사용, 검출만 가능하며 복구는 불가능
2) Standby Sparing : 결함 감지 위한 여분 H/W
3) TMR (Triple modular Redundancy)
- 3개 이상의 프로세서가 같은 입력에 대하여 동일한 연산 수행. 다수결 판정회로(ajority voter) 사용, 비행기/지휘통신 시스템
4) Watchdog Timer : 주기적 타이머 가동을 통한 초기화, 단단계/다단계
4) Self Purging Redundancy : 출력결과가 다른 HW 를 계산과정에서 배제

# S/W 측면 기법
1) Check pointing : 검사시점에서 오류가 발결되면 이전의 검사시점으로 되돌아가서 재수행
2) Recovery Block(RB) 
- 검사시점에서 오류가 발견되면 지정된 이전 검사점으로 되돌아가서 같은 기능을 가진 다른 S/W 모듈을 수행
- N번 모두 실패시 Exception Handler 호출, old 버전과 new 버전 교체시 유용
3) Conversation : 재수행(Rollback & Retry)에 근거한 Recovery BLock 의 확장형
4) Distbuted Recovery Block : Recovery Block 기법을 분산환경으로 확장
5) N Self-checking Programming
- N 버전에 대한 각각의 SElf-checking 컴포넌트(Acceptance Test)가 수행 - 결과비교후 출력결정 - NVP, RB 보다 오버헤드 발생
6) N version Programming(NVP) : N 개의 독립적인 S/W 모듈의 수행결과 비교하여 다수의 수행결과 채택(HW 의 TMR 을 SW 로 구현)
```


#### [85/134] FT(Fault Tolerant) 2
```aiignore
# DataBase 측면 기법
1) Rollback(Undo)
2) Log File : Commit 안된 작업을 로그파일로 회복
3) Check Point : 중간 검사시점을 두어 발견시점 이후만 회복
4) Shadow paging : 트랜잭션 실행동안 수행 페이지 테이블과 그림자 페이지 테이블을 지속 유지 

# Data 측면 기법
1) Parity Code : 2진 정보 내의 "1" 의 개수를 홀수 또는 짝수로 규정하여 결함 감지
2) M of N Code : N bit 의 정보길이에서 "1" 의 개수가 반드시 M 개가 되게 하여 결함감지
3) Checksum : 한블록의 정보내용 합을 추가
4) Berger Code : 한정보의 "1" 의 개수를 2진 정보로 추가
5) Hamming Error Correcting Code : 결함감지 및 단일결함의 교정까지 수행하는 코드기법
```


#### [86/134] HA(High Availability)
```aiignore
1. 최소한의 서비스 중단을 통한 연속성 확보, HA
- 두대 이상의 시스템을 클러스터로 구성
- 한 시스템의 장애시 다른 시스템이 서비스를 Failover, 최소한의 서비스 중단을 구현한 기술


2. HA 의 구성유형(H, M, C) : keep alive packet 활용
1) Hot Standby : 가동시스템 + 백업시스템, 외장 Disk 는 가동시스템 접근 가능, 장애시 백업시스템 접근가능
2) Mutual Takeover : 2대의 가동시스템, 장애시 상대방 자원을 failover, 외장 Disk 는 해당 시스템만 접근가능
3) Concurrent Access : N 개의 시스템 병렬 업무처리, 장애시 failover 하지 않고 가용성 보장


3. HA 의 상세 구현 방식
1) Hot Standby : 한 서버 서비스중 (DB+Web) - 다른서버 대기(DB + Web) -> 장애 발생 -> 대기중이던 서버 동작
2) Active - Active (Load Balancing) : 서로 다른 서비스를 운영하다가 장애가 발생하면 상호 역할을 대신 할 수 있는 구성
3) 전용대기 : 별도의 전용 대기서버 존재 
4) 상호대기
- 서버1(DB-Active), 서버2(Web-Active, DB 대기), 서버3(DB/Web-대기, 파일 active) 구성
- 앞단계의 서비스 장애시 뒷단계에서 대응할 수 있는 구성
5) 분산 오류 복구 : 주서버 장애 -> 개별서버 동작
6) 공유디스크가 없을 경우의 방법 : 평상시 실시간 데이터 복제하고 장애시 Data Mount 서비스


4. HA 동작방식
1) 시스템 전체 장애시 
- 한 시스템으로부터 keep alive packet 이 오지 않는 경우 백업시스템은 상대 시스템이 Down 되었다고 판단
- 정의된 자원(Volume Group, File System, IP Address, Application)을 Failover

2) 네트워크 Adapter Card 장애시
- Service Adapter 에 장애가 발생하면 Standby adapter 가 Service dapter 의 IP 주소를 Failover

3) TCP/IP 네트워크 장애
- N/W 전체 장애시에는 백업시스템으로 Failover 하더라도 동일한 N/W 문제가 존재함(네트워크도 이중화 구현 필요)


5. HA 한계점
1) External Disk 자체 장애발생시 HA Solution 으로 해결 못함
2) 시스템 성능이 저하되는 경우에 자동감지가 불능(장애발생으로 인한 Down 만 감지)
3) DB 및 Application 이 Down 되는 경우에는 일반적으로 failover 하지 않음
4) HA 구성에 따른 정보교환으로 시스템의 안정성, 보안성, 성능에 오버헤드가 존재함
- Keep Alive Packet / Heart Beat Check   
```


#### [85/134] Virtualization(가상화)
```aiignore

```
