


#### [94/134] GC(Garbage Collection)
```aiignore
1. 불필요한 메모리 수고, GC
- Java Application 구동시 더 이상 참조하지 않는 메모리를 자동수거하는 JVM의 주요기능

2. GC 동작 매커니즘
2.1 Minor GC(Young, Old) 
- Copy & Scavenge
- 전제조건(Eden : 객체 생성시 저장되는 영역, Survivor : GC 발생시 Eden 객체가 이동, 반드시 하나는 "비움" 상태여야 함
1) 객체생성시 Eden 에 저장
2) GC 발생 : Eden + SS1 -> SS2 로 이동(SS1 비움상태)
3) 객체생성 : Eden 에 저장
4) GC 발생 : Eden + SS2 -> SS1 으로 이동 (SS2 비움상태)
5) 1~4 반복과정중 오래된 객체는 Old 로 이동

2.2 Full GC(Old 영역의 GC) : Mark & Compact
1) 전체 객체의 Reference 를 따라가면서 연결되지 않은 객체 Mark
2) Parallel GC : Multi Thread 를 이용 병렬적으로 GC 수행

3. GC 의 주요 알고리즘
3.1 Minor GC
1) Copy & Scavenge : 고속 작은 크기의 메모리 collection 에 효과정
2) Parallel GC: Multi Thread 병렬적으로 GC 수행

3.2 Full GC
3) Mark & Compact : 객체 사용유무 Mark 하고, Mark 된 객체 삭제
4) Concurrent GC : Full GC 시 멈춤현상 최소화, App 동작중에도 일부 GC 수행

3.3 기타
5) Incremental GC : Minor GC 수행시 Full GC 조금씩 수행
6) G1 Collector : concurrent GC 대체목적, 대용량 메모리/다중 프로세스서버 플랫폼 환경용
```


#### [95/134] System tuning 
```aiignore
 
```


#### [96/134] 컴파일러 / 인터프리터 
```aiignore
 
```



#### [97/134] TMR(Triple Modular Redundancy)
```aiignore
 1. Voting 을 통한 정상여부 판단, TMR 정의
 - 동일한 모듈 3개 구성하여 Major Voter 를 통한 하나의 모듈의 정상여부 판별
 - 하나의 모듈 에러 발생시, 나머지 2개 Vote 에 의해 결정하는 결함허용 시스템
 
 2. 특징
 1) 안전 무결성 : 삼중화 시간여분 기법을 활용하여 무오류 및 연속 프로세스 운영 제공 
 2) 높은 가용성 : 모듈이 고장이 나더라도 시스템이 작동하는 동안 교체 가능
 3) 다른 벤더 제품 : 보통 3개의 모듈을 서로 다른 벤더의 제품으로 구성
 
 3. 구성요소
 1) Input
 2) Module
 3) Mayor Voter
 4) Output
```


#### [98/134] UNIX
```aiignore
 1. UNIX
 - 미국의 Bell 연구소에서 Multics OS 를 대체하기 위해 C + Assembly 로 개발된 범용 OS
 
 2. UNIX 특징
 1) shell 기반(대화식 운영체제, 멀티 태스킹)
 2) C 언어 기반(높은 이식성)
 3) utility 포함(코드 개발 필요 유틸리티 포함)
 4) 높은 안정성 (서버 + 임베디드)
 
 3. UNIX 구성요소
 1) 커널
 2) 쉘
 3) 시스템콜 
 4) 파일시스템
```


#### [99/134] Unix File System
```aiignore
1. 유닉스 파일 시스템
- 유닉스 운영체제가 파티션이나 디스크에 파일을 할당하고 이를 다시 회수하는등 디렉토리 정보를 관리하는 파일시스템 구조
 
2. 유닉스 파일시스템 논리적 구조 및 구성요소
1) 부트블록 : 부팅시 필요한 코드를 저장하고 이는 블록 / Bootstrap code
2) 슈퍼블록
가. 파일시스템의 상태를 관리하고 저장하는 공간/파일시스템 상태
나. 총 i-node수, 파일 시스템 크기, 블록/단편크기, 그룹당 inode 수, 마지막 마운트쓰기/시간, inode 테이블 크기, free 블록리스트
3) inode (index node) 블록
- 각 파일의 위치정보, 파일 관련 정보저장, 파일 위치정보, 파일크기, 데이터위치
- 파일유형, UID, GID, 사용하가권, 생상날자, 변경시기, 최근사용시기
4) 실린더 그룹 블록 : 실린더 그룹내의 유효 블록들의 비트맵 정보나 통계 정볼르 기록
5) 데이터 블록 : 실제 데이터가 저장되는 공간 / 데이터 저장 
```


#### [100/134] inode 
```aiignore
1. 파일, 디렉토리 정보보유, inode
- 유닉스에서 파일이나 디렉토리의 모든 정보(id, owner, access mode 등)을 가지고 있는 자료구조

2. inode 가 관리하는 정보
1) 소유자 정보 : 사용자 ID, 그룹 ID
2) 접근정보 : access modes, data block address(13개의 array)
3) 파일정보 : 크기, 생성시간, 최근사용시간, 최근변경시간, 파일링크수, 유형(ordinary 파일, special 파일, directory 파일)

3. inode 구성요소
1) inode : 구성정보 / 한 파일이나 디렉토리의 모든 정보 포함
2) inode table : 하위 노드 정보 / 파일이나 디렉토리들의 전체 inode 를 갖고 있는 테이블
3) inumber : 등록정보 / inode 가 ilist 에 등록되는 엔트리 넘버
4) address : 실제 데이터 정보
- direct data block 10개(0~9) : 데이터 블록 주소 저장
- single indirect data black(10) : index 블록을 가리킴, 인덱스 블록은 실제 데이터 블록 포인터
- double indirect data block(11) : 인덱스 블록이 2개의 레이어로 구성
- triple indirect data bock(12) : 인덱스 블록이 3개의 레이어로 구성

4. inode 의 역할
1) 할당 및 적용 : inode 는 파일의 구성블로록에 대한 물리적 위치 포함 / 직접, 간접 data block
2) 파일 생성 : 신규 파일 생성시, 해당하는 inode 가 ilist 안에 생성됨 / inumber 와 파일이름
3) 파일링크 : 파일링크, 디렉토리에 해당 파일에 대한 새로운 이름이 등록됨 / inumber 와 복사
4) 파일삭제 : 파일삭제시 inode 의 파일링크수가 하나 감소됨 / inumber zero 처리 
```


#### [101/134] Linux
```aiignore
 
```
