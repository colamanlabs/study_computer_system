


#### [51/134] Cache Memory(Locality)
```aiignore
1. 캐시 히트율 극대화를 위한 지역성

2. 지역성 활용 목적
1) 캐시 접근시간 최소화
2) 캐시 적중율 최대화
3) 캐시 Hit Ratio 최대화 

3. 지역성 종류
1) 시간적 : 최근 사용 데이터가 참조될 가능성이 높음 -> 순환, 서브 프로그램
2) 공간적 : 요청주소 인접데이터들이 참조될 가능성이 높음 -> 배열, Prefetch
3) 순차적 : 기억장치에 저장된 순서대로 순차적으로 인출, 실행될 가능성 -> 명령어 순차실행

4. 지역성을 이용한 관리방법
1) 기억장치 계층구조 : CPU 레지스터(내부), 캐시(외부) -> access time 줄이고 지역성 이용, cache miss 감소 
2) 캐시 억세스 시간 : 캐시 히트율 극대화 
3) 워킹 셋 : 자주 access 하는 페이지들의 집합  

5. 지역성 적용사례
1) 캐시 메모리 : CPU > 캐시 > 메모리 -> 시간(최근 가장 적게 사용한 block 교체), 공간(prefetch)
2) 가상 메모리 : 시간(워킹셋을 이용한 페지 교환 최소화, LRU, FIFO, LRU 등 시간, 공간 참조지역 활용 
3) CDN 서버 
```


#### [52/134] Cache Memory 의 Coherence(일관성)
```aiignore
1. 캐시 메모리의 코이허런스(일관성)
- 멀티 프로세스로 인한 로컬 캐시 메모리와 공유 메모리의 데이터 불일치 방지하고, 일관성을 유지하는 기법

2. 캐시메모리 일관성 유지 기법
1) 공유 캐시 사용 : 모든 프로세서가 하나의 캐시 사용 -> 프로세서의 충돌이 심함
2) 공유 캐시 변수 미사용 : 공유변수를 캐시에 저장하지 않으므로 일관성 유지 하지만 성능 저하 
3) 버스감시 매커니즘 적용(스누프 제어기) : 각 캐시 제어기에 버스 감시 기능을 수행하는 스누프 제어기를 추가하여 데이터 일관성 유지 
- 쓰기 갱신 : 모든 캐시에 갱신정보 보내짐, 쓰기 무효 : 다른 캐시 블록 무효화 시킨후 쓰기 동작 배터적 수행
4) 디렉토리 기반 일관성 유지 프로토콜 : 캐시 전역정보 상태를 디렉토리에 저장하여 일관성 유지  
```


#### [53/134] Cache Memory(Mapping)
```aiignore
1. 캐시매모리 패밍
- 캐시 용량이 메인 메모리보다 작어서 메인 메모리의 일부분만 캐시로 적재되므로, 최고의 적중률을 내기 위해 매핑 필요

2. 캐시매모리 매핑기법(히트율 향상을 위핸 매핑 알고리즘 수행)
1) 직접사상 
- 메모리의 각 블록이 캐시의 특정 라인으로 적재
- 회로구현이 용이, 간단, 캐시메모리와 1:1 대응
- 메모리주소 = tag + line + word

2) 완전 연관 사상
- 메모리의 각 블록이 캐시의 어느 라인에나 적재가 가능
- 캐시 적중률이 높음
- 회로 복잡, 처리속도가 느림 
- 메모리주소 = tag + word

3) 집합 연관 사상
- 캐시를 N개 세트로 나누어 각 세트는 직접 사상으로 매핑
- 캐시와 메모리가 M:1 대응(적중률 향상)
- 메모리 주소 = tag + line + word  
```


#### [54/134] Cache Memory(교체알고리즘)
```aiignore
1. 캐시 메모리 교체알고리즘
- 캐시 메모리의 용량이 한계에 다다랐을때 그 내용을 교체하는 알고리즘
1) LRU : 사용되지 않고 가장 오래 머물러 있던 블록 교체
2) LFU : 가장 사용빈도가 낮은 블록을 교체 
3) FIFO : 캐시내에 가장 오래 머물러 있던 블록을 교체 
4) 임의(random) : 블록을 임의로 선택
5) Optimal : 향후 가장 참조되지 않을 블록 교체

2. 캐시 메모리 효율적 관리방안
1) 기술적 측면
- 시스템 성능 저하 예방 
- 교체전략 및 알고리즘 고도화
- 최적의 캐시 사이즈 확정

2) 관리적 측면
- 캐시 히트율 관리
- 캐시 성능 튜닝
- 중장기계획 예산수립
```


#### [55/134] Cache Flush, Cache Clean
```aiignore
1. Cache Flush
1) 캐시 메모리 세트, 전체를 무효화 하고 주 메모리에서 다시 읽어들일때 메모리에 데이터 저장없이 캐시를 비우는 동작의 관리기법
2) 소프트웨어 방식으로 진행(I-cache, D-cache, I/D 캐시중 하나 선택후 실행)

2. Cache Clean : 캐시의 내용을 메모리에 업데이트 하는 메모리 관리 기법

3. 용어 : valid bit, dirty bit
```


#### [56/134] MESI Protocol 
```aiignore
1. 캐시 일관성 유지
- 멀티 프로세서 시스템에서 캐시 일관성을 유지하기 위하여, 메모리가 가질 수 있는 4가지의 상태를 정의한 캐시 일관성 유지 프로토콜 

2.MESI Protocol 특징
1) write back 프로토콜 : 캐시 내용을 변경해도 바로 주기억장치는 갱신 안함
2) 스누프 제어기 : 캐시의 제어기가 변경사실을 다른 캐시에 알려줌

cf) 스누프 제어기는: 메모리 버스의 모든 읽기/쓰기 트랜잭션을 감시합니다.

3. 4 가지 상태 및 개념도
1) 수정(Modified) : 캐시의 데이터가 수정 또는 변경된 상태, 주기억장치와 내용 상이
2) 배타(Exclusive) : 캐시 데이터가 유일한 복사본이고, 주기억장치의 내용과 동일
3) 공유(Shared) : 특정 데이터가 2개 이상의 캐시에 적재
4) 무효(Invalid) : 2개 이상의 캐시에 적재되었던 데이터가 한쪽에 의해 변경되어 무효화  
```



#### [57/134] Virtual Memory
```aiignore
1. 주소공간의 제약 개선, Virtual Memory
- 주기억장치에서 처리되는 Data 양이 많아질 때, Throughput 향상을 위해 보조기억장치의 일부분을 주기억장치처럼 사용하는 가상의 메모리 활용 기법

2. 가상 메모리 구성기법
1) 페이징 : 동일크기 블록 -> 페이지 사상표
2) 세그먼트 : 가변크기 블록 -> 함수, 모듈단위, 세그먼트 사상표
3) 혼합 방법 : 세그먼트후 페이징 -> 혼함, 혼합 사상표

3. 사상 기법
1) 직접사상 : RAM -> 메모리내 1:1 사상 테이블 관리, 성능 비효율
2) 연관사상 : TLBs -> 동시접근 가능한 고속캐시, 연관장치, 고비용
3) 혼용방법 : locality -> 지역성 원리, 히트율 높은 것 위주의 TLB 관리 

4. 관리정책
1) 할당정책(How Much) : 각 프로세스에게 할당할 메모리의 양을 관리 
-> 고정할당, 가변할당 기법

2) 호출정책(When) : 언제 어느항목들을 보조기억장치에서 메인메모리에 가져올지 결정
-> 요구호출(Demand Fetch), 예측호출(Pre-Fetch)

3) 배치정책(Where) : 프로그램의 한 블록을 메인메모리의 어디에 배치할 것인가 관리 
-> First Fit, Best Fit, Worst Fit

4) 교체정책(Who) : 메인메모리에 적재할 공간이 없을 경우 무엇과 교체할 것인가 
-> 무작위, FIFO, LFU, LRU, NUR(NRU, Not Recently Used), MFU (Most Frequently Used), OPT(Optimal, 앞으로 가장 오랫동안 사용되지 않을 페이지 제거)

5. 가상기억장치 사용시의 문제점 / 해결방안
1) Demand Paging 
- 요구될 때에만 프로세스의 페이지들을 적재하는 방식 -> 참조의 지역성 활용

2) Page Fault Trap 
- 기억장치에 적재되지 않은 Page 를 사용시 발생
- 페이지 유무 판단, 빈공간 검색, 디스크로부터 읽어옴. 명령 재수행

3) 스레싱 발생 : Page 부재가 빈번하게 발생하여, CPU 가 프로세스 수행보다, Page 교체에 더 많은 시간을 소요하는 비정상 적인 현상 발생
- locality model 사용 : 프로그램들은 일정시간동안 일정한 장소들만 집중적으로 사용하는 원리의 이용
- working set : 일정시간동안 참조되는 페이지의 집합을 주기억장치에 유지
- page fault frequency : page fault 빈도에 따라 residence set(상주집합) 을 조정. PFF(Page Fault Frequency) 가 높으면 Residence set 크기 증가, 낮으면 줄임

cf) Residence Set : 프로세스에게 현재 할당된 page frame 집합  
```


#### [58/134] Virtual Memory 의 관리정책 
```aiignore
1. Virtual Memory 관리정책 유형
1) 할당정책(How Much) : 각 프로세스에게 할당할 메모리의 양을 관리 
-> 고정할당, 가변할당 기법

2) 호출정책(When) : 언제 어느항목들을 보조기억장치에서 메인메모리에 가져올지 결정
-> 요구호출(Demand Fetch), 예측호출(Pre-Fetch)

3) 배치정책(Where) : 프로그램의 한 블록을 메인메모리의 어디에 배치할 것인가 관리 
-> First Fit, Best Fit, Worst Fit

4) 교체정책(Who) : 메인메모리에 적재할 공간이 없을 경우 무엇과 교체할 것인가 
-> 무작위, FIFO, LFU, LRU, NUR(NRU, Not Recently Used), MFU (Most Frequently Used), OPT(Optimal, 앞으로 가장 오랫동안 사용되지 않을 페이지 제거)
```

#### [59/134] Virtual Memory(페이징 교체 알고리즘)
```aiignore
1. Virtual Memory 페이지 교체 알고리즘
1) 무작위 교체
2) FIFO
3) LFU
4) LRU
5) NUR
6) MFU(Most Frequently Used)
7) OPT 

2. FIFO Anomaly(피포 이상현상) 
- FIFO 방식의 교체에서 페이지 프레임수를 늘려도(메모리를 늘려도), Page Fault 가 오히려 증가하는 현상(Belady's Anomaly-벨라디 이상현상)
- 원인 : FIFO 교체 알고리즘의 locality 미고려로 인한 치명적 취약점 -> LRU 교체알고리즘이 고안된 원인
- FIFO Anomaly 해결방안
    - 페이지 교체방식 변경 : LRU, OPT
- 최적화 원칙 설계 : locality 활용

3. SCR(Second Chance Replacement) : 각 페이지마다 참조비트를 두고 FIFO 기법을 이용하여 페이지 교체 수행
- 참조 비트가 0 일때는 교체, 1일때는 참조비트 0 으로 지정후, FIFO 맨 마지막 리스트로 피드백 시켜 다음순서 기다리게 하는 알고리즘
```



#### [60/134] Virtual Memory(페이징 기법)
```aiignore
1. Virtual Memory(페이징 기법)
- 가상 메모리에서 고정된 하나의 크기의 프레임으로 따라 나누는 방식 -> 매핑 테이블(페이지번호, 프레임 번호)
- 블록이 일정한 경우 페이지(page) 라고 하고, 그와 관련된 가상 기억장치 구성을 페이징 이라고 함

2. Virtual Memory 페이징 특징
1) 외부 단편화 해결 가능, 내부 단편화 발생(고정크기)
2) 프레임 크기에 따라 단편화 정도와 관리 오버헤드간 트레이드오프 있음
3) 프로그램의 실제 주소와 주기억장치의 주소가 다름 -> PMT(Page Map Table)이 필요 

3. Virtual Memory 페이징 기법의 종류
1) 직접사상(Direct Mapping)
- RAM, Page Table 을 주기억장치에 위치
- Page Table 과 Data 에 각가 한번씩 주기억 장치 접긎

2) 연관사상(Associative Mapping) 
- TLBs, Page table을 Associative buffer 에 저장 -> 속도가 빠르고 가격이 비쌈

3) 직접 / 연관 혼합사상 
- Page table 을 주기억장치와 Associative buffer 에 분산저장 -> 경제성과 성능 절충
```

#### [61/134] Virtual Memory(세그멘테이션)
```aiignore
1. Virtual Memory 세그멘티이션기법
- 메모리를 서로 크기가 다른 논리적인 블록단위인 "세그먼트" 로 분할하고, 메모리를 할당하여 논리주소로 변환

2. 세그멘테이션기법 특징
1) 논리적인 의미에 따라 서로 다른 크기를 가지는 논리적인 블록
2) 메모리 사용할 시점에 할당
3) 연속적인 공간에 배치 

3. 세그멘테이션 구조 및 용어설명
1) 세그먼트 : 메모리 영역에 나뉘어 있는 영역을 세그먼트
2) 세그멘테이션 : 메모리를 영역별로 쪼개는 것을 세그멘테이션
3) 세그먼트 테이블(segment table) : 세그먼트 시작주소(base) + 세그먼트 길이(limit) 정보 테이블
4) offset : 각각의 세그먼트 관리 단위
- offset 으로 관리하고 논리영역으로 나누어 놓으면 물리적 위치 변환으로 활용가능 -> 관리수월

4. 세그먼트와 페이지 주소 변환 용어
- r(물리주소) = p(프레임번호) + d(offset)

5. 세그먼트 성능 저하 방안
1) 페이징 TLBs 적용
2) 전용 세그먼트 레지스터 도입
```

#### [62/134] 세그먼테이션 오류(Segmentation Fault)
```aiignore
1. 잘못된 메모리 참조로 인한 오류, 세그먼테이션 오류
- 프로그램이 접근 권한이 없는 메모리주소(세그먼트)에 접근하려고 할 때, 운영체제가 발생시키는 런타임 오류

2. 세그먼테이션 오류 발생원인
1) 널포인터 역참조
- NULL 로 초기화된 포인터가 가리키는 주소에 접근시도
2) 버퍼오버플로우
3) 댕글링 포인터
4) 읽기전용 영역쓰기 

3. 세그먼테에션 오류 해결방안
1) 코어덤프 분석
2) 동적분석 : 런타임 검증
3) 정적분석 : 코드레벨 검증, 소나큐브등
4) 코드인스펙션 : 코드검증 

4. 세그먼테이션 오류 예방기법
1) 널포인터 방어(Null Pointer Check)
2) 경계검사(Boundary Check)
3) Use-After-Free 방지 
- free() 호출후, 포인터를 즉시 NULL 로 초기화 하여 댕글링 포인터 방지
4) 초기화 
- 포인터 변수 선언시 즉시 NULL 또는 유효한 주소로 초기화
```



#### [63/134] MMU(Memory Management Unit)
```aiignore
1.  가상메모리와 실제 메모리 사이의 주소변환을 위한 MMU
- MMU 는 가상메모리 주소를 실제 메모리 주소로 변환하는 하드웨어 장치 

2. MMU 구성요소
1) Adress Translation : 가상메모리 주소를 실제 접근할 수 있는 물리 메모리 주소로 변환
2) Privilege Control : 사용자 프로그램에서 커널 영역에 침범하지 못하게 막아주는 역할
3) Cache Control : 영역에 따라 캐시가 가능한 영역과 가능하지 않은 영역을 설정, 제어
4) Read Protection : Read 할 수 없는 영역을 생성
5) Write Protection : Write 할 수 없는 영역을 생성
6) Memory Protection : 각 프로세스 별로 자기 영역만 액세스 할 수 있도록 하는 기능
```



#### [64/134] TLBs(Translation Lookaside Buffer)
```aiignore
1. TLBs : 페이지 테이블 항목의 연관캐시이며, 가상주소가 매핑될 때 마다 주메모리에 엑세스 할 필요가 없도록 하는데 사용
2. TLBs 의 역할 : MMU 는 TLBs(캐시)를 사용하여 가상 주소 변환 속도를 향상 
```



#### [65/134] Thrashing
```aiignore
1. Thrashing
- 가상 메모리에서 페이지 부재(Page Fault) 가 비정상적으로 많이 발생하여 페이지 교체에 따른 프로세스 처리가 급격히 저하되는 현상

2. Thrashing 원인
1) 리소스 부족
2) 부적절한 페이지 교체(locality 미고려)
3) 과도한 멀티프로그래밍(수용 가능한 교체수 초과)

3. Thrashing 해결기법 
1) Working-Set Model
- 프로세스들은 주어진 시간동안 자신의 페이지들중 일부에 접근하려는 locality 가 발생하며, 이를 통해 부재율을 감소시킴
- 매번 참조시마다 Resident Page Set 조정 -> PFF 보다 오버헤드가 큼

2) Page-Fault Frequency
- 페이지 부재율의 상한과 하한 설정을 관리
- Working Set Model 보다 더 직접적으로 페이지 부재율 조절
- 페이지 부재 발생시 Resident Page Set 조정

4. Thrashing 회피를 위한 고려사항
1) Prepaging : 페이지 부재 방지를 위해 관련된 모든 페이지를 사전에 모두 메모리에 로드함
2) 페이지 크기 조정 : 페이지 크기를 조절함으로서, 스레싱을 조정할 수 있음
3) 프로그램 구조 개선 : 프로그램 개발시 지역성 고려, 개발 언어에 따른 특성 고려
4) 페이지 잠금 : 각 페이지마다 잠금비트(lock bit) 를 사용, 잠금비트가 ON 인 경우 교체 불가 
```


#### [66/134] Memory Device(RAM)
```aiignore
1. 메모리(RAM, ROM)
- RAM
    - DRAM
    - SRAM
- ROM
    - Mask ROM
    - PROM(Programmable ROM) : 1회기록 가능
    - EPROM(Erasable Programmable ROM) : 삭제 가능(UV 로 전체삭제)
    - EEPROM(Electrically Erasable Programmable ROM) : UV 불필요, 소프트웨어적으로 갱신가능
- Flash Memory  

2. 메모리 계층구조
- 고가의 고속 메모리와 저가의 저속 메모리를 계층적으로 배치, 최적가격에 컴퓨터 성능을 향상시키는 컴퓨터 아키텍처
1) 가격대비 높은 성능, 빠른 Device 의 성능 보장(장치간 속도차이를 중간속도 메모리를 통해 보완)
2) 용량, 비용, 성능의 상관관계(tradeoff) 를 고려하여 하이러라키 구성
```

#### [67/134] 메모리 인터리빙 
```aiignore
1. 접근시간 향상을 통한 메모리 성능 향상, 메모리 인터리빙
- 메모리 접근시간을 최소화 하기 위해, 메모리를 복수개의 모듈로 나누고 각 모듈에 연속적인 주소를 부여하여 동시에 접근이 가능한 기법

2. 메모리 인터리빙 사용 목적
1) 주메모리의 평균 전송율 향상
2) 메모리 대역폭 확대
3) 메모리 접근시 병목현상 완화

3. 메모리 인터리빙의 활용방식
- 데이터들을 기억장치에 분산 저장하는 방식에 따라 구분
1) 상위 인터리빙 : 기억장치 주소를 모듈들에 순차적으로 지정, 상위비트(모듈선택신호), 하위비트(모듈내 기억장소)
2) 하위 인터리빙 : 기억장치 주소를 모듈단위로 인터리빙, 사우이 비트(모듈내 기억장소), 하위비트(모듈 선택신호)
3) 혼합 인터리빙 : 기억장치 모듈들을 뱅크로 그룹화, 뱅킹선택시 상위 인터리빙 사용, 뱅킹내 모듈간 하위 인터리빙

4. 메모리 인터리빙의 access 활용방식
1) C-엑세스(Concurrent-access) : 주소들이 프로세스-기억장치간 버스를 통해 순차적으로 기억장치 모듈에 도착하는 방식
2) S-엑세스(Simultaneous-access) : 모든 기억장치 모듈들에서 읽기 동작이 동시에 시작, 읽혀진 데이터를 순차적으로 전송하는 방식
```


#### [68/134] 메모리 단편화
```aiignore
1. 기억장치 낭비현상, 단편화
- 분할된 주기억 장치에 프로그램 할당, 반납과정에서 사용되지 못하고 낭비되는 기억장치가 발생하는 현상

2. 단편화 종류
1) 내부 단편화 : 분할된 영역이 프로그램보다 커서 프로그램 할당후 사용되지 않고 남는 공간
2) 외부 단편화 : 분할된 영역이 프로그램보다 작아 프로그램 할당 불가로 사용되지 않고 남는 공간

3. 기억장치 분할유형과 단편화
1) 고정분할 : 주기억장치를 고정된 크기로 분할하여 프로그램 할당 -> 내부, 외부 단편화 모두 발생 가능(내부 심각)
2) 가변분할 : 주기억장치의 분할영역이 동적으로 변경 가능 -> 내부 단편화 없음. 외부 단편화 발생

4. 단편화 해결방안
1) 통합 : 인접된 영역에 빈 기억장소 존재시 하나의 공백으로 합치는 기법
2) 압축 : 공백들의 재배치로 하나의 큰 공백을 만드는 방법. GC 기법(모든 작업 중지, 작업에 대한 재배치 정보 유지 필요)
3) 프레임 기법 : 메모리를 일정한 크기로 나누어 관리하여 내부 단편화 해결
- 프레임 : 물리 메모리를 일정한 크기로 나눈 블록
```



#### [70/134] Flash Memory 1
```aiignore
1. 부유게이트 방식의 비휘발성 메모리, Flash Memory
1) 기계적인 동작이 없는 순수 반도체로 이루어진 비휘발성 저장매체, 속도가 빠르고 전력소모가 작으며 또한 부피가 작고 충격에 강한 메모리
2) 1984년 토시바 가 개발, 이전 정보를 지우는 시간이 EPROM 에 비해 크게 짧아서 번쩍(Flash) 하는 순간에 끝난다는 의미에서 붙여짐

2. 특징
- 비휘발성, 고집적도, 저전력, 고 신뢰성

3. 플래시 메모리 셀구조
- 산화물 층으로 절연된 플로팅 게이트에 전자를 주입시켜 반영구적으로 저장하는 원리, 1bit 의 데이터를 저장

4. 회로구조에 따른 플래시 메모리의 구분
1) NOR 플래시 메모리 : 바이트 단위 어드레싱, 고속의 리딩, BIOS, 휴대폰
2) NAND Flash Memory : 블록단위 어드레싱, 상대적 빠른 소거, 쓰기, SSD, 대용량 저장장치 

5. NAND 플래시 메모리 생산방식
- SLC, MLC, TLC
```
