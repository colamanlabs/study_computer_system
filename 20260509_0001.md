

#### [87/134] Virtualization(가상화)
```aiignore
1. 가상화
- 물리적으로 서비스, 장비를 통합하고 논리적으로 재구성하여 자원 활용율을 향상시키는 소프트웨어 기술


2. 가상화의 원리
1) Sharing
- 물리적으로 위치한 자원을 사용자에게 나누어 사용할 수 있도록 함 / 파티셔닝, VLAN

2) Aggregation
- 분산자원을 통합하여 논리적으로 단순화하여 자원의 활용율을 높이고 관리를 용이하게 함 / 클러스터링

3) Emulation 
- 가상화로 인한 논리적 객체는 물리적인 객체와 동일한 기능의 수행이 가능함 / VTL, 에뮬레이터

4) Insulation
- 물리적인 자원의 교체나 변경에도 서비스를 안정적으로 유지함 / RAID, HA, L4 스위치


3. 가상화 유형
1) SW 가상화 : OS Emulation(JVM), Resource Management(PRM), Virtual OS(VMWare)
2) HW 가상화 : partioning(IBM LPAR, IBM vPAR ESX 서버)
3) SW + HW 가상화 : Distributed Workload Management(IBM bWLM)
 
 
4. 가상화 유형별 구현기법
1) 데스크탑 가상화
- PC 에서 이루어지는 모든 작업이 중앙 가상화 서버에서 이루어지게 함 / SBC,ThinClient, NW 컴퓨팅
2) 서버 가상화
- 하나이상의 SErver 물리적인 자원들을 논리적으로 통합 / Physical / Logical Partioning
3) Storage 가상화
- 복잡하게 구성되어 있는 Storage 자원들을 간단하게 보여주는 기술
4) NW 가상화 
- 물리적 NW 자원들을 마치 하나의 자원처럼 사용 / VPN, VLAN, MPLS
5) OS 가상화
- 하나의 PC 에 서로 다른 OS 를 설치, 사용 / VMware, Hyper-V
```




#### [88/134] Server Virtualization
```aiignore
1. Server virtualization
- 서버의 물리적인 자원들을 논리적으로 통합하여 마치 하나의 서버를 통해 서비스를 제공하는 기술

2. 도입효과
1) 기존서버에 비해 운영비용의 절 감가능 / 대부분의 서버가 10% 미만의 사용율 - 60 ~ 80% 사용율 향상
2) Partition 을 통해 여러개의 서버를 단일 서버에 구축함으로서 서버 Consolidation(강화, 통합) 구축 가능
3) 가상화 기술을 사용함으로서 자원(특히 CPU)에 대한 공유가 가능하므로 비용 효율적임
4) 친환경 측면 : 전력/열/공간/탄소배출 감소, 서버투자 및 관리비용 효율성 증대
5) 재무적 측면 : 유휴 서버 자원 재사용을 통해 가동률의 향상 및 별도의 투자부담을 최소화
```



#### [89/134] VDI(Virtual Desktop Infrastructure)
```aiignore
1. VDI (Virtual Desktop Infrastructure)
- 논리적인 서버 기반 데스크톱 가상화
- HW, OS, SW 까지 모두 가상화된 환경에서 제공(일반 PC 에서 SW 방식으로 제공)

2. SBC(Server Based Computing)
1) 서버기반 가상화 컴퓨팅(일반적인 터미널 PC, Thin PC, zero PC 개념)
2) 서버의 리소스를 활용하는 가상화 방식, VDI 나 application 가상화까지 포함
3) 리소스 및 데이터 중앙관리로 보안강화 및 패치수월, 관리효율, 단말기 사양 낮아도 됨

3. CBC(Client Based Computing)
1) 클라이언트 기반 가상화
2) PC 에서의 가상화를 통한 사무환경 분리와 망분리 구현이 가능함 (PC 내의 가상의 OS 생성 활용)
```


#### [90/134] 하이퍼바이저(Hypervisor)
```aiignore
1. 하이퍼바이저(Hypervisor)
- OS 와 H/W 사이의 가상화 층으로 다수의 OS 들 사이에서 리소스, 사용량, 스케쥴링을 담당하는 Virtual Machine Manager

2. 하이퍼바이저 유형

1) Type1(Bare Metal)
- Host OS 가 따로 없이 Hypervisor 가 해당 하드웨어에서 직접 실행(VMware ESX, Citrix Xen Hypervisor)
- 오버헤드 적음 / 물리적 컴퓨터 리소스의 관리 유연, 좀더 높은 성능(HW 까지 경로 짧음)

2) Type2(Hosted)
- H/W Layer 위에 Host OS 가 실행, 호스트 OS 에 의해 Hypervisor 실행구조 (MS Hyper-V, User Mode Linux)
- Guest OS의 종류에 제약 없음(Windows, FreeBSD, Linux 등), Hypervisor 설치만으로 가상화 용이 
```


#### [91/134] Storage Virtualization
```aiignore
1. 스토리지 가상화(Storage Virtualization)
- 물리적 스토리지의 구성/위치에 관계없이 독립적인/논리적인 스토리지 환경을 구성, 통합 볼륨관리, 동적 할당이 가능한 서비스 인프라

2. 스토리지 가상화의 구성요소
1) Block 단위 가상화
- 호스트가 LUN 을 인식하지 않고 가상화 솔루션이 제공하는 LUN 을 인식, 개별 시트로지 장비에 종속되지 않음
2) 스토리지디바이스 자원관리
- 하나의 화면에서 기종, 업체에 관계없이 모든 관리작업(RAID 구성, 패스설정 등)을 수행 할 수 있는 솔루션
3) 블록단위 파일공유
- 대용량 고속 IO 블록처리를 위한 SCSI level 의 블록공유

3. 스토리지 가상화의 구현방식
1) 설치 위치에 따른 가상화 구축방식 : 호스트 기반, 스토리지 기반, 네트워크 기반
# 호스트 기반
- 호스트(서버)에 가상화 SW 를 설치하고 호스트와 연결된 디스크의 가상화
- 스냅샷 방법을 통해 데이터 미러링을 지원
- 다양한 볼륨정책도 가능
- 부하발생의 문제점
- 중소규모 단일 OS 환경에 적합

# 어레이 기반
- 다양한 이기종 서버의 연결을 원하는 스토리지 환경에 초점
- 가상화를 통한 동적 볼룸 확장
- 스냅샷, 클로닝등 내부 복제기술 제공으로 중요기능의 단순화
- 어레이에 장착된 디스크를 이용한 데이터들의 분산 저장
- 성능 및 안정성 우수

# 네트워크 기반 
- 논리적이며 단순한 관리 제공
- 스토리지 네트워킹, 자원공유 연결성의 확장, 성능/용량 확장
- 관리비용의 증가문제 해결
- 기술한계로 미션크리티컬 업무에 적용하기는 어려움
- App 시스템의 사용경우와 특정 호스트 SAN 스위치 상에 설치되는 솔루션으로 구분 


2) 처리 방식에 따른 가상화 구축방식 : In-Band 방식, Out-of-Band 방식
# In-Band 방식
- 개념 : 서버와 디스크간 IO 에 가상화장비 개입
- 고성능 서비스 : IO 성능병목, 지연증가
- 서비스 가용성 : 시스템, OS 제약적 가용성
- 데이터 정합성 : 디스크 캐시 / 데이터 동기
- 확장 / 유연성 : 제약적 확장, 비표준화

# Out-of-Band 방식
- 개념 : 서버와 디스크간 IO 에 가상화 장비 미개입
- 고성능 서비스 : Wire0-Speed 고성능 서비스 
- 서비스 가용성 : SAN 레벨 고가용성
- 데이터 정합성 : No 캐시 / 데이터 동기
- 확장 / 유연성 : SAN 의 고확장성, 표준화 
```




#### [92/134] Clustering
```aiignore
1. HA 의 핵심기술, Clustering
- 여러대의 컴퓨터를 연결하여 하나의 대형 시스템을 사용하는 것과 같은 기능을 제공하면서 고가용 요구사항을 만족시키는 컴퓨팅 기술

2. 클러스터링
- 가격대비 성능비 우수 / 시스템 유지비용의 감소 / 프로그램 개발 용이

3. 클러스터링 핵심기술
- 고가용성(고장시 다른 서버 수행)
- 병렬 및 부하균등화
- 노드확장에 따른 성능의 선형증가(확장성)
- Single System Image

4. 클러스터링의 종류
1) Active / Standby 클러스터 : 가장 단순한 방식으로 한대의 시스템은 운영하고, 나머지는 전원을 ON 되어 있지만 서비스는 안올려진 상태
2) Active / Active 클러스터 : 양쪽 모드 동일하게 서비스 제공, 문제 발생시 상호 백업체제로 구성
3) Shared Everything 클러스터 : 두개의 시스템 사이에 있는 디스크 데이터를 같이 액세스 하는 구조 
4) Shared Nothing 클러스터 : 하나의 물리적 디스크를 두개의 논리적 디스크로 분할
5) Shared Memory 클러스터 : 한 시스템이 Fail 시 FailOver 시간이 발생하면 무결성 확보를 위한 메모리 sharing 이 적용된 기술

5. 클러스터 기술 적용사례 및 비교
1) 계산용 클러스터
2) 부하 분산용 클러스터
3) 고가용성 클러스터 및 FT
- CPU, 메모리를 가진 각 SMP 노드들이 공유디스크를 사용하여 하나의 시스템 이미지를 가짐
```


#### [93/134] JVM (Java Virtual Machine)
```aiignore
1. Java 프로그램 실행도구, JVM(JAVA Virtual Machine)
- 자바소스코드의 컴파일 결과인 바이트코드를 읽어 실행시켜주는 메모리 상에만 존재하는 가상 컴퓨터

2. JVM 의 구성도 및 구성요소
1) 클래스 로더 : class 파일을 메모리에 올리는 기능(Loading) / 플랫폼 독립성
2) 실행 데이터 영역 : 클래스 파일이 실행하면서 활용할 데이터 보관 / Method, Heap, Stack, Native Method 스택, PC 레지스터
3) 실행 엔진 : 자바 프로그램을 수행시킴 / 파일 해석기
4) 가비지 컬렉터 : 힙 메모리의 비사용영역 관리(삭제) / Minor GC, Full GC 

3. 실행순서
1) Java Source : 자바로 작성된 코드(.java)
2) Java Compiler(javac) : 기계가 이해하는 언어로 번역(byte code)
3) JVM : 플랫폼 환경에서 실행(.class)
```


